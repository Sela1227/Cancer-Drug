# SELA-handoff.md — 健保癌症藥物速查系統

> 給 **Kit Claude**（升 Kit 用）。本專案首次對齊 SELA Kit（V1.9.0），依規範必產出 handoff。
> 分類為我（專案 Claude）初判，**請 SELA 確認第二節「通用坑」與第五節「不要回流」的歸類是否正確**。

---

## 〇、專案速覽

- **專案名稱：** 健保癌症藥物速查系統（Cancer Drug Lookup）
- **專案類型：** 純靜態單檔 HTML（GitHub Pages，前置密碼閘）+ GitHub Actions 上游監控
- **技術棧：** 原生 HTML/CSS/JS 單檔；Python（GitHub Actions 抓 PDF 用）；無 build、無依賴、無後端
- **規模：** 1 個 index.html（~1.2 MB，含 149 筆藥物資料 + 衛教庫 + 樣式 + 邏輯）+ .github/
- **使用 Kit 版本：** V1.9.0（首次對齊）
- **完成版本：** V4.1.3（對齊 Kit + 115/6/1 法規更新）
- **完成日期：** 2026-06-01

---

## 一、用 Kit 的整體感受

### 預期外的順利
- 三位版號規則 §11.2「逢十進位」清楚，連既有專案「保留歷史 + 從下版開始嚴格」都有明確指引，落點（V3.x 超過 .9 → V4.0.0）判得出來。
- 既有專案最小對齊清單（鐵律 7 項）讓「該補什麼」一目了然。

### 預期外的卡住
- 既有專案從非三位（V3.10/3.11/3.12）遷到三位時，「下版落點」仍有一點模糊（保留歷史的情況下，要不要把 12 正規化）。最後跟 SELA 對焦才定 V4.0.0。建議 Kit 補一個「既有專案版號遷移落點」的小範例。

### 整體評價
- ✓ 鐵律對齊清單 + 三位版號規則實用、可直接照做。
- ✗ 缺「既有專案版號遷移落點」的具體範例（本案 V3.12 → V4.0.0 可當第一個案例）。

---

## 二、發現的「跨專案通用坑」（建議進 Kit）

> 以下為「上游文件 / 資料驅動型專案」共通，非本專案獨有。每條附本專案證據。

#### 1. 上游文件更新要做「兩層 diff」（編號層 + 內容層）
- **症狀**：只比對「項目編號出現/消失」會漏掉「既有編號內部的文字修訂」。本案漏掉 10+ 個既有藥物的內部條文（含一條關鍵的免疫治療新適應症）。
- **原因**：diff 方法論只做編號層。
- **做法**：兩層 diff — (1) 編號層比新增/刪除；(2) 內容層把每個編號段落忽略空白後逐條比，並掃描所有生效日標記找新條文。
- **影響範圍**：任何「定期跟上游文件（法規/規格/字典/價目表）更新」的專案。
- **證據**：本專案 CLAUDE.md 坑 #30。

#### 2. 改了結構化資料，衍生索引（search_text 等）一定要重生
- **症狀**：手工改了顯示用的結構化欄位，但搜尋索引欄位沒同步重生 → 索引與顯示不一致（一個藥的索引塞進另一個藥的內容；新加的資料索引為空）。
- **原因**：兩個欄位來自同一份資料，改一個沒重生另一個。
- **做法**：把衍生欄位的產生寫成單一函式，任何資料異動後一律全量重生 + 逐項回歸驗證格式（本案 143/149 逐字一致才放心全量重生）。別只靠 runtime fallback。
- **影響範圍**：任何有「結構化資料 + 衍生索引/快取/全文欄位」的專案。
- **證據**：本專案 CLAUDE.md 坑 #31。

#### 3. OCR 來源的 diff 要過濾控制字元/空白假陽性
- **症狀**：兩次掃描同一份文件，OCR 把連字號讀成控制字元（`\u0002`）或加減空白，造成「內容沒變卻被判為修訂」的假陽性。
- **原因**：逐字比對沒正規化 OCR 雜訊。
- **做法**：比對前正規化（去空白、控制字元還原為 `-`、去標點）；對「被判修訂但無生效日標記」者人工複核。
- **影響範圍**：任何以 OCR 文字為來源做 diff 的專案。
- **證據**：本專案 CLAUDE.md 坑 #30（9.71 假陽性案例）。

---

## 三、發現的「跨專案設計模式」（建議進 sela-philosophy / 規範）

### 1. 「DIFF 報告先確認、再重建」
- **本案情境**：上游更新後不直接改，先產出 DIFF 報告給 SELA 確認變動範圍，確認後才動資料。
- **可推廣原則**：資料驅動專案的更新流程＝「偵測 → 產差異報告 → 人確認 → 重建」，把人的確認放在重建之前。
- **代價**：多一次來回；但避免重建錯方向、避免靜默漏改。
- **建議寫入**：start-project-decisions.md 或 sela-philosophy.md。

### 2. 「寬驗證：逐項比對權威來源」
- **本案情境**：SELA 選「寬」後，把 149 筆全部逐藥拿去比對權威 PDF，順手抓出 3 類既有 bug（與本次更新無關）。
- **可推廣原則**：做局部更新時，若成本允許，做一次「全量對權威來源」的驗證，常能撿出歷史遺留 bug。
- **代價**：較久；需有可機讀的權威來源。
- **建議寫入**：cross-project-pitfalls.md 或 sela-philosophy.md。

---

## 四、Kit 該瘦身或調整的地方

#### 1. deployment/SPEC.md §11 — 補「既有專案版號遷移落點」範例
- **現狀**：有「保留歷史 + 從下版開始嚴格」原則，但沒有「V3.12（非三位）→ 下一版該落在哪」的具體算例。
- **建議改成**：加一個算例，例如「既有專案最後一版 V3.12，下次為功能版 → 因 b 早已超過 .9，依逢十進位落 V4.0.0」。
- **理由**：本案就在這個落點卡住、需跟 SELA 對焦。

---

## 五、留在這個專案、**不要回流 Kit** 的東西

- 健保署「藥品給付規定第 9 節」的解析規則（章節 regex `^\s{0,3}9\.\d+\.`、日期戳格式、(刪除) 保留編號等）— 業務邏輯。
- 9.69 免疫檢查點抑制劑的 PD-L1 表結構、事審代碼（P001…P091）、per_cancer 逐癌切片資料 — 業務模型。
- 藥物 schema（num/name/brand/cancers/types/items/subitems/per_cancer/common_data/search_text）— 該專案特有資料結構。
- cancer_keywords 自動切片字典、癌別衝突排除規則（神經內分泌/GIST 等）— 業務細節。
- 139 藥衛教庫六段式內容 — 臨床業務內容。

---

## 六、Kit Claude 的建議行動清單

### 建議升 Kit 版本
V1.9.1 或 V1.10.0 — 判斷：若只補 §11 範例（1 處）→ c+1；若同時收 3 條通用坑 + 2 個設計模式 → b+1（結構性新增）。

### 必做
- [ ] deployment/SPEC.md §11 加「既有專案版號遷移落點」算例（本案 V3.12 → V4.0.0）。

### 建議（待 SELA 對焦通用性）
- [ ] cross-project-pitfalls.md 收坑：上游更新兩層 diff、衍生索引必重生、OCR 假陽性過濾。
- [ ] sela-philosophy / start-project-decisions 收兩個設計模式：DIFF 報告先確認、寬驗證對權威來源。

### 不做
- [ ] 健保第九節解析規則、9.69 表結構、藥物 schema 進 Kit（業務邏輯，不通用）。

---

## 七、最後備註

本案是該專案首次對齊 Kit，同時夾帶一次真實法規更新（115/6/1），所以反饋兼含「對齊感受」與「資料驅動更新流程」兩類。三條通用坑都已在專案 CLAUDE.md 坑 #30–#32 留有完整三段式紀錄，可直接引用驗證。
