# SELA-handoff.md — 健保癌症藥物速查系統（Cancer Drug）

> 給 **Kit Claude**。本份為累積式 handoff，依 Kit V1.26.0 分段鐵律標記
> `#### 本次新增`（V5.2.0–V5.4.0 區間新產生的提案）與 `#### 歷史保留`（前幾版已提、僅列管追蹤）。
> **scanner 與 Kit Claude 只需處理「本次新增」。**

---

## 〇、專案速覽

- **專案名稱：** Cancer Drug（健保癌症藥物速查系統）
- **專案類型：** 純靜態單檔 HTML（GitHub Pages branch-serve，前置密碼閘）+ GitHub Actions 上游監控
- **技術棧：** 原生 HTML/CSS/JS 單檔；Python（建置端腳本、不進 repo）；無 build、無依賴、無後端
- **規模：** 1 個 `index.html`（~1.16 MB，含 151 筆藥物資料 + 衛教庫 + 樣式 + 邏輯）+ `.github/`
- **使用 Kit 版本：** V1.27.0（上次對齊 V1.9.0，落差 18 個 b 版）
- **完成版本：** V5.4.0
- **完成日期：** 2026-07-30
- **本次 handoff 涵蓋區間：** V5.2.0 → V5.4.0
- **三層目標歸屬：** 醫院發展（癌症中心個管師日常查核工具）

---

## 一、用 Kit 的整體感受

### 這次對齊的形態跟前幾次不同

落差 18 個 b 版，但**跳版落差評估的收穫不在補格式**。Kit V1.27.0 已經把本專案 V5.1.0 的教訓收成坑 #83 與 #7 補強——回流這端已閉環、確實流上去了（回應 Kit V1.26.0「接收端說明」想解的痛點：**本專案這端確認機制有效**）。

真正撿到的三件事，全都是**舊規範一直有、但沒人回頭核對**：

1. 「關於本系統」面板停在 V4.x 的「逐癌切片」世界觀（架構改了三個大版、說明文字沒人動）
2. `.gitignore` 專案特定段在保護 V5.0.0 已確認不存在的檔案
3. 兩處 catch 區塊的 `console.log`

→ **跳版落差評估的實際價值＝用一份新清單強迫自己重看一遍**，而不是「Kit 又多了什麼規定要補」。建議把這點寫進 claude-init 第二章該情境，避免下個長壽專案把它當成補格式作業草率跑過。

### 預期外的順利

- V1.26.0 的「優化體檢技術棧前篩」直接有效：純靜態專案一行剔掉 OPT-1/2/3，只實對照 1 條，沒有 CCM 反映的空轉。
- V1.25.0「§17.2 傾向提示不進精煉 prompt」的澄清救了一次錯：我原本已在對話中給 SELA 一份 **300 字英文 prompt**（含 mood 形容詞、negative prompt、指定主體「放大鏡＋膠囊」、寫 sibling to SELA logo）——§14.7 六條守則全違反。讀到規範後整份重產成 ≤100 字中文精煉版。

### 預期外的卡住

- 見第四節第 1 條（§14.7 守則的位置問題）。

---

## 二、發現的「跨專案通用坑」（建議進 Kit）

#### 本次新增

##### 1.【A】改架構時，「說明自己怎麼運作」的靜態文案必須進同步清單

- **症狀關鍵字**：關於頁、說明文字、幫助文案、架構已改文案沒改、自我描述
- **症狀**：程式架構連改三個大版（V5.0.0 移除切片、V5.1.0 標籤重推、V5.2.0 表格結構化），但「關於本系統」面板仍寫「逐癌切片狀態：9.69(手工)、其他自動切片／尚未切片：9.5、9.16、9.36」，資料日期也停在兩版前。使用者讀到的「這個系統怎麼運作」是**已被廢除的架構**。
- **原因**：說明文案是「描述系統的第二份真相」，但它不在任何同步清單裡，也**沒有任何測試會抓**——程式正確、資料正確、文案說謊。UI 版號檢查（SPEC §10.6）也抓不到：版號是對的。
- **做法**：把「自我描述型文案」（關於頁 / 說明面板 / README 運作說明 / 首頁介紹）列進專案 CLAUDE.md 的發版同步清單，與資料日期、版本號同級。判準：**這段文字有沒有在描述「系統內部怎麼做的」？有 → 進清單。**
- **跟既有的差別**：#3 是「資料與顯示同源」（同一份資料兩處讀）；#79 是「數字型單一真相」（數字要標源頭）。本條講**架構敘述型文案**——它描述的不是資料、是設計本身，兩條都沒涵蓋。
- **證據**：本專案 CLAUDE.md 坑 #44。

##### 2.【F】`stopPropagation()` 擋不住元素自己的 handler——註解寫了不等於防到了

- **症狀關鍵字**：stopPropagation、冒泡、capture、按鈕連帶觸發父層、註解說謊
- **症狀**：卡片標題列上的按鈕點下去，除了執行自己的動作，還連帶展開／收合卡片。程式裡明明有 `e.stopPropagation(); // 防止觸發卡片展開`。
- **原因**：`toggle` 綁在 `.card-header` 元素上（target 層），而 `stopPropagation()` 寫在 **document 層冒泡階段** 的 listener 裡。冒泡順序是「先跑 target 上的 handler → 再往上到 document」，所以卡片早就被 toggle 過了。**同一份程式裡另外三顆按鈕沒事，因為它們用的是 capture 階段的 document listener**——這個對比讓 bug 更難被發現（「別的按鈕都好，一定不是這原因」）。
- **做法**：要擋「元素自己的 handler」只有兩條路——(a) 用 **capture 階段** listener，或 (b) **在那個 handler 內自己排除**（`if (e.target.closest('.btn-a, .btn-b')) return;`）。並且：**改成手風琴（一次只開一個）會讓這類 bug 從「多開一個」升級成「還把別人關掉」**，所以動展開邏輯時要一起檢查。
- **附帶紀律**：本 bug 由外部審稿指出，但在專案裡躺了多版沒人發現——因為註解看起來已經處理了。**註解宣稱的行為要驗證，不能當成已實作。**
- **證據**：本專案 CLAUDE.md 坑 #43。

##### 3.【A】上游轉檔把表格壓平，會被誤判成「前端排版問題」；找同類問題要用可量化指標掃

- **症狀關鍵字**：PDF 轉文字、表格壓平、內文雜亂、矩陣、重複片語、短行比例、round-trip
- **症狀**：使用者反映「內文雜亂」，第一反應是調 CSS。實際是來源 PDF 的一張 29 列 × 6 欄矩陣在轉文字時行列全黏成一段（3,901 字），而且矩陣裡多數格子是「不適用」的雜訊——前端無論怎麼排版都救不回來。
- **原因**：結構在資料層就已丟失。也因此上一代只能靠「人工摘要」繞過，讓摘要與原文成為兩份真相（呼應 #83）。
- **做法**：
  1. **解析回結構**：找穩定錨點（本案是表格內的送審代碼 `P\d{3}`）、確認儲存格用語是**封閉集合**（20 種，長詞優先逐格比對）、跨頁重複的欄位標題列先移除。
  2. **強制 round-trip**：組回原字串必須**逐字一致**才可出版——否則等於重新輸入一份臨床資料。
  3. **呈現只顯示有意義的格子**，完整原表另置可展開區（本案 6 欄裡通常只有 1–3 格有內容）。
  4. **掃「還有沒有第二張」要用可量化指標**：把來源切段，對每段算 (a) 最長重複片語出現次數、(b) 短行比例。壓平的矩陣在這兩個指標上極端突出（本案 82 次 / 66%，第二名 9 次 / 21%），一眼判定全文只有一處。
- **附帶紀律**：逐字元對比來源與成品時，**一定要先把「可解釋的差異」分類扣掉**（條號、標題行、日期註記、字元正規化），否則報告 90% 是誤報、看兩眼就會放棄。本案未分類時 468 筆「疑似缺失」，分類後真正的只有 4 筆。
- **證據**：本專案 CLAUDE.md 坑 #41、#42。

#### 歷史保留

- 上游文件更新要做「兩層 diff」（編號層 + 內容層）→ **已整合**
- 改了結構化資料、衍生索引一定要重生 → **已整合（Kit #47）**
- OCR 來源的 diff 要過濾控制字元 / 空白假陽性 → **已整合**
- 衍生資料兩鐵則（UI 讀原文、遷移輸入用原始權威來源）→ **已整合（Kit #83，V1.27.0，與 OncoPA 併為 N=2）**
- 資料合規驗證會擋出版、BLOCK/WARN 分級 → **已整合（Kit #7 補強，V1.27.0）**

---

## 三、發現的「跨專案設計模式」（建議進 sela-philosophy / 規範）

#### 本次新增

### 1. 「還有沒有其他地方也壞了」要用可量化指標去掃，不能靠肉眼翻

- **本案情境**：發現一張表格被壓平後，設計兩個指標（重複片語次數 × 短行比例）掃過 153 個來源段落，確認全文只有一處。
- **可推廣原則**：修完一個個案，下一步是「**設計一個能找出同類個案的指標**」，而不是憑印象宣稱「應該只有這一處」。指標要能產出可比較的數字，才有「一眼看出離群值」的效果。
- **代價**：要花時間想指標；指標選錯會漏。但比全文肉眼翻可靠得多。
- **建議寫入**：`sela-philosophy.md`（可與五.5 失效模式節相鄰——「主觀認定只有這一處」是同類失效）。

### 2. 跳版落差評估的價值＝用新清單重看自己，不是補格式

- **本案情境**：Kit 落差 18 個 b 版，新規範幾乎都已符合；真正撿到的三件事全是舊規範一直有、沒人回頭核對的。
- **可推廣原則**：落差評估時，除了逐條看「Kit 新增了什麼」，要**另外跑一次完整鐵律核對**（把專案當第一次看）。前者找新規範，後者找舊債——後者收穫往往更大。
- **建議寫入**：`templates/claude-init.md` 第二章「已對齊過但 Kit 跳版」情境，補一行說明與判準。

#### 歷史保留

- 「DIFF 報告先確認、再重建」→ 已提
- 「寬驗證：逐項比對權威來源」→ 已提
- 「設計失敗模式的方向」（選出錯方向對使用者傷害較小的設計）→ 已提
- 「資料合規要有一支會讓建置失敗的腳本」→ 已提（V1.27.0 已整合進 #7）

---

## 三.5、發現的「程式碼優化」（建議進 optimizations.md）

#### 本次新增

無新提案。本次優化體檢結果見第六節「優化體檢」——OPT-4 已實質落實、OPT-1/2/3 技術不適用、**0 項可升級**。

---

## 四、Kit 該瘦身或調整的地方

#### 本次新增

### 1. logo/CLAUDE.md：§14.7 守則的位置太後面，容易「先寫錯、再讀到」

- **現狀**：`logo/CLAUDE.md` 全長 1,200+ 行，§14.7 精煉 prompt 守則在第 834 行、§17 自動工作流在第 957 行。Claude 若被 SELA 直接問「給我 logo prompt」而沒先讀這份檔，**極可能憑常識產出英文長 prompt**（本案就發生了：300 字英文、含 mood / negative prompt / 指定主體 / reference，六條守則全違反）。
- **建議改成**：在 `logo/CLAUDE.md` §0「預設行為」加三行硬性導引：
  ```
  SELA 說「給我 logo prompt」→ 不要憑常識寫，先讀 §14.7 + §17。
  精煉 prompt 只有兩塊：app 介紹 3-4 句 + 設計精神。中文、≤100 字、不指定主體。
  必須產出檔案 SELA-logo-prompt.md（§17.4 鐵律），不是只在對話裡回答。
  ```
- **理由**：master CLAUDE.md §0 第 3 點只說「必含 logo（整合方式見 logo/CLAUDE.md）」——沒有任何訊號告訴 Claude「產 prompt 這件事有嚴格格式、且必須產檔案」。守則寫得很好，但**放在讀者已經寫完之後才會讀到的位置**。

### 2. master CLAUDE.md §10 交付清單，建議加一條「自我描述型文案是否同步」

- **現狀**：§10「改舊版的額外檢查」四條都是 CLAUDE.md 內部的（坑 / 版本歷程 / 下版工作 / 一句話總結）。
- **建議加**：`[ ] 程式內的「關於 / 說明」文案還在描述現在的架構嗎？（架構改版時最容易漏）`
- **理由**：本案關於面板停在三個大版前的架構，§10.6 的 UI 版號檢查抓不到——版號是對的、文案是錯的。

#### 歷史保留

- SPEC §11 補「既有專案版號遷移落點」範例 → 前幾版已提（本專案 V3.12 → V4.0.0 可當案例）

---

## 五、留在這個專案、**不要回流 Kit** 的東西

- 健保署「藥品給付規定第 9 節」的解析規則（章節 regex `^\s{0,3}9\.\d+\.`、民國日期戳格式、`(刪除)` 保留編號等）— 業務邏輯。
- 9.69 免疫檢查點抑制劑的 PD-L1 表結構、送審代碼（P001…P151）、病名→癌別對映表 — 業務模型。
- 藥物 schema（`num/name/brand/lead/revisions/cancers/types/items[cancerTags]/subitems[cancerTags,table]/search_text`）— 該專案特有資料結構。
- 「行政共通條文」關鍵字表（`UNIVERSAL_A` / `UNIVERSAL_B`）與「標題定調適應症、傘狀條文才用子項標籤」的語意規則 — 法規文本特有。
- 139 藥衛教庫六段式內容 — 臨床業務內容。
- 主題色 `#436f8a`（非 Kit 預設 `#5A7A8B`）— 已上線驗證的事實標準，依對齊 SOP 鐵律第 2 條保留。

---

## 六、Kit Claude 的建議行動清單

### 建議升 Kit 版本

**V1.28.0**（b+1）— 3 條新坑 + 2 個設計模式 + 2 處 Kit 調整建議 = 新內容，無不相容變更。

### 必做

#### 本次新增

- [ ] `cross-project-pitfalls.md` 收 3 條新坑：架構敘述型文案要進同步清單、`stopPropagation` 擋不住自身 handler、上游表格壓平的偵測與 round-trip 紀律。
- [ ] `logo/CLAUDE.md` §0 加「產 logo prompt 前必讀 §14.7 + §17、且必須產檔案」三行導引（第四節第 1 條）。
- [ ] master `CLAUDE.md` §10 加「自我描述型文案是否同步」一條（第四節第 2 條）。

#### 歷史保留

- [ ] SPEC §11 加既有專案版號遷移落點算例 — 前幾版已提，仍待處理。

### 建議（待 SELA 對焦通用性）

#### 本次新增

- [ ] `sela-philosophy.md` 收「用可量化指標找同類個案」設計模式。
- [ ] `claude-init.md` 第二章「已對齊過但 Kit 跳版」情境補「落差評估的價值＝用新清單重看自己」。

### 優化體檢（V1.17.0 鐵律，本次結果）

```
4 條 OPT 中 3 條技術不適用（純靜態單檔 HTML、無後端 / 無 DB / 無 runtime 外部請求）、
實際對照 1 條：

- OPT-1 系統 cron → threading.Timer：技術不適用（無專案內排程；GitHub Actions cron 屬平台層）
- OPT-2 散落種子資料 → master JSON：技術不適用（無 DB seed）。附註：V1.25.0 補的取捨
  「純靜態無 build 不該抽 JSON」正好描述本專案現況（資料內嵌 index.html）＝現況已是建議寫法
- OPT-3 直接送外部請求 → outbox queue：技術不適用（runtime 無外部請求）
- OPT-4 生成後配自我驗證迴圈：已實質落實，不重複立法——
  validate 腳本 BLOCK/WARN 分級（BLOCK 任一筆就不出版）、PD-L1 表 round-trip 逐字比對、
  lead 必須逐字存在於原文、資料筆數斷言、151 藥 × 6 癌別實跑渲染、node --check 全 script

→ 0 項可升級。這是「已檢查、確認不用改」的結論，不是沒檢查。
```

### 不做

- [ ] 健保第九節解析規則、9.69 表結構、藥物 schema、病名對映表進 Kit（業務邏輯，不通用）。

---

## 七、給 Kit Claude 的最後備註

本份是本專案第二次 handoff。第一次的提案已在 Kit V1.27.0 全數處理完畢（#83 升格、#7 補強），**回流機制在本專案這端確認有效**——可作為 Kit V1.26.0「接收端說明」機制的正向驗證案例。

本次新增的三條坑都不是「新技術踩的新坑」，而是**長壽專案特有的失效模式**：程式一直在進化、但描述程式的東西（說明文案、忽略清單、註解）留在原地。如果 Kit 想收成一個更上位的原則，可考慮：

> **專案裡所有「描述系統」的東西都會腐化，而且不會有任何測試抓到它們。**

本份第二節第 1 條、第四節第 2 條、以及坑 #43 的「註解宣稱的行為要驗證」都是它的實例。
