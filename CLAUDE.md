# CLAUDE.md — 健保癌症藥物速查系統

> 給下次接手這專案的 Claude。讀完這份能直接動手，不需要從頭問。

---

## 一、這是什麼

單檔 HTML 工具,給彰濱秀傳癌症中心的個管師(Sela 等 3 人)查健保署藥品給付規定第 9 節抗癌瘤藥物。資料源是健保署 115 年 4 月 23 日更新的 PDF。佈署在 GitHub Pages public repo,前面掛了一個防君子的密碼閘。

整個專案就一個 HTML 檔加上 GitHub Actions:
- `index.html`(約 1.06 MB;V5.0.0 移除重複的 per_cancer 切片後由 1.24 MB 縮減)
- `.github/workflows/check-nhi-update.yml` + `.github/scripts/check_nhi.py`:每週狙擊健保署網站,有更新自動下載 PDF + 開 Issue + 寄 email

**沒有 build process、沒有依賴、沒有後端**。

**核心臨床價值**:選擇癌別後只看到該癌別相關條件,避免在 24,000+ 字長條文(如 9.69)中迷路。

**V5.0.0 起改用條文標籤(`cancerTags`)**：不再另存 `per_cancer` 切片,而是**每條原文貼癌別標籤、篩選只篩原文**。通用條件標 `"*"`(所有癌別都顯示)。失敗模式因此從「切片漏條=畫面藏條」翻轉成「頂多多顯示幾條」。詳見坑 #37。

---

## 一之二、衝突仲裁（刻意不對齊 Kit 的決定）

| 項目 | 本專案的決定 | 理由 |
|------|------------|------|
| **App logo** | `favicon/app-logo*`（膠囊＋雷達靶心，霧藍 `#436f8a`）**兩套母圖**：≤32px 雷達簡化版、≥48px 完整版 | 設計來源＝ Sela 用 Gemini 生成 + Claude 優化轉檔（Kit logo §10.2 工作流 B）|
| **雙 logo 制** | app logo＝主視覺（favicon / 入口頁 / 工具列）；SELA logo＝歸屬印記（入口頁底部、主畫面頁尾、關於面板底部）| Kit logo §9；app logo 永遠主視覺、SELA 永遠輔助，不並排同尺寸（64:18 ＝ 3.6:1）|
| 主題色 | `#436f8a`（非 Kit 預設北歐霧藍 `#5A7A8B`） | 已上線驗證的配色是事實標準;對齊 SOP 鐵律第 2 條禁止「為對齊 Kit 而改既有設計」 |
| 專案名稱 | 英文 `Cancer Drug`、UI 與文件用中文「健保癌症藥物速查系統」 | SPEC §10.5 只約束檔名/路徑,畫面顯示層可中文 |
| 建置端腳本 | 不進 repo（列在 `.gitignore`） | `index.html` 是唯一交付產物;腳本與稽核檔留在對話端,避免 repo 混入非部署檔 |

---

## 二、單一真相對映表

| 設定項 | 位置 | 改的時候要對齊什麼 |
|--------|------|------|
| 密碼 | `index.html` 內 `PASSWORD_HASH` | `echo -n "新密碼" \| sha256sum` |
| 主色 | `:root --primary: #436f8a` | 連帶改:`theme-color`、入口頁/body 漸層、FAB shadow、search shadow |
| 藥物資料 | `index.html` 內 `const drugs = [...]` | 151 個藥物項目 |
| 癌別篩選 | 每條 `items[].cancerTags` + `subitems[].cancerTags` | **由 `derive_v51.py` 從原文重推,不手改**;不確定一律 `["*"]` |
| 病名→癌別對映 | `derive_v51.py` 的 `DISEASES` | 新癌別/新病名在這裡加;器官單詞要用「須帶癌症字尾」的正則 |
| 行政條文判定 | `derive_v51.py` 的 `UNIVERSAL_A` / `UNIVERSAL_B` | A 級無視癌名一律 `["*"]`;B 級只在無癌名時才 `["*"]` |
| 資料合規 | `validate_v51.py` | **BLOCK 有任何一筆就不得出版**;WARN 只產報告 |
| 藥名 / 商品名 | `drugs[].name` / `.brand` | **必須逐字出現在健保署原文**(validate BLOCK 10,坑 #49);不是「metadata」,是抽取欄位 |
| 條號對齊官方 | `items[].num` | 無編號前導句一律進 `lead`,**不可佔用 `1.`**(validate BLOCK 12,坑 #50) |
| 收錄政策 | 官方「（刪除）」條號一律收錄 | 完整收錄法(validate BLOCK 11);目前 9.7 / 9.84 / 9.89 |
| **全文差異回歸** | `diff_source.py` -> validate BLOCK 13 | **漏抄/疑錯字/未收錄條號必須全為 0 才可出版**(坑 #51) |
| **標籤規則一致性** | `rederive.py` -> validate BLOCK 14 | **出版檔標籤必須等於規則重推結果**;繞過 `derive_v51.py` 手改會直接擋出版(坑 #53) |
| 父項目條文 | 一律由子項目重建 | 父落後於子會靜默顯示舊條文(坑 #51,9.5 實證) |
| 過濾條件 | 全部進 `getFiltered()` | **不可渲染後用 CSS 隱藏**,否則統計數字與 pill 計數會說謊(坑 #52) |
| 黏著 header | `.header-stack{position:sticky;top:0}` 包住 topbar + 搜尋列 | **`top` 不可寫死常數**(那是「另一個元素多高」的隱含假設,坑 #55) |
| 資料日期文案 | `.brand-data-date` 內含 `.dd-long` / `.dd-short` | 屬性 `data-date`/`data-effective` 被 JS 讀,**改文案時不要動屬性** |
| 搜尋正規化 | `normSearch()` 與 `highlightRe()` | 兩者必須共用同一套規則,否則「找得到卻沒反黃」(坑 #52) |
| 原文自我矛盾 | 子項的 `xrefNote` / 表格列的 `rowNote` | **不改條文**,以註記呈現官方跨章節差異(坑 #50 ①②) |
| 9.69 PD-L1 對照表 | `parse_pdl1.py` -> 子項的 `table` 欄位 | 條文更新後要重跑;**round-trip 不一致就不可出版**;儲存格用語有新增要補 `CELLS` |
| 藥品層前言 / 歷次修訂 | `extract_lead.py` -> `lead` / `revisions` | 從原文抽取,不手填;`lead` 必須逐字存在於原文(BLOCK 檢查) |
| **「關於本系統」面板文案** | `index.html` info-panel | ⚠️ **改架構 / 換資料日期時必須同步改這裡**(坑 #44:它曾停在 V4.x 的切片世界觀) |
| 顯示層字元轉換 | `footnoteMark()`(`*`→上標 ※)、`formatHierarchy()`(`數字*10數字`→`×10ⁿ`) | **純顯示、不動資料**;資料層必須與健保署原文逐字一致(坑 #45) |
| 複製條文純文字 | `buildClauseText()` / `plainText()` / `tableToText()` -> `window.cdqClauseText` | 顯示層轉換規則要與畫面一致;**空白收斂必須在插入縮排之前**(坑 #46) |
| SELA 歸屬印記（3 處）| 入口頁 `.gate-credit`、主畫面 `.app-footer`、關於面板 —— 皆 `.sela-credit` -> `favicon/sela.svg` | **引用獨立圖檔**,不可重用 favicon 引用(Kit logo §9);**尺寸必須寫在 CSS**(sela.svg 只有 viewBox,坑 #48) |
| **App logo（雙軌）** | `favicon/app-logo*.png` / `app-logo.svg` | **唯一的簡化幾何在 `app-logo.svg`**(分頁 SVG + 36px 工具列);**所有光柵檔一律由完整版母圖縮**(方案 A,坑 #54);SELA logo 只用於歸屬印記 |
| theme-color | `index.html` meta + `favicon/site.webmanifest` 的 `theme_color` | 兩處必須一致,且用 **app 主題色 `#436f8a`**(不是 SELA 橘) |
| app logo prompt | `SELA-logo-prompt.md` | 依 Kit §17.4;改顏色/範本時重產,不手改 prompt |
| 事前審查（跨癌別）| **載入時由條文推導** `paClausesFor()` / `_paByCancer` / `_paCancerScope` | **事審是條文層要求,不是藥品層屬性**(坑 #56);不新增資料欄位 → 條文更新後自動正確 |
| 事前審查（父/子藥物）| `pre_review` / `review_scope` / `review_source` | 全部由程式推導,不人工填。**注意 `review_scope` 講父子、`_paCancerScope` 講跨癌別,是兩件事** |
| 通用條件 | `cancerTags: ["*"]` | 取代舊 `common_data` 欄位(V5.0.0 起移除) |
| 9.69 標籤 | **無特案** —— 由通用規則處理(子項標題自帶病名,`scan_diseases` 抓得到) | V5.0.0 曾有 `MAP_969` 對映表,**V5.1.0 起已不在管線中**(坑 #53 的腐化來源) |
| **資料當前版本** | `.github/scripts/check_nhi.py` 的 `CURRENT_DATA_DATE` | **每次重建資料時更新成新的 YY.M.D** ⚠️ |
| Logo | `data:image/jpeg;base64,...` | 三處引用同一變數 |

---

## 三、踩過的坑（編號累積，永不重排）

1. **檔名用中文 → GitHub Pages URL 變編碼地獄**  → 永遠用 `index.html`

2. **PDF 章節標題前面有空格 → regex 抓不到**  → regex 改成 `^\s{0,3}9\.\d+\.`

3. **SubtleCrypto 在 file:// 不可用**  → 純 JS SHA-256 fallback

4. **藥物父項目自己沒有適應症內文**  → V3.3 父項目展開時把子項目內容合併呈現,並做精細逐癌切片

5. **多個 sticky 元素 z-index 競爭**  → filter-panel: 300、filter-overlay: 250、topbar: 200

6. **桌機 close button 被一起隱藏**  → 用 ID 限定 `#filter-panel .close-btn`

7. **PDF 條文裡有日期戳干擾**  → regex `\s*[（(][\d/、]+[）)]` 全部移除

8. **藥物分類有交集**  → types 是 array

9. **【V3.1】用截斷字數處理長條文**  → 改用結構化解析 + 逐癌切片

10. **【V3.1】主編號判斷需驗證連續編號**  → 用 `next_main_num`,不靠縮排

11. **【V3.1】1280px 寬螢幕版面失調**  → max-width 改 1400px

12. **【V3.2】跨癌藥物即使結構化展開仍混亂**  → 選癌別時只渲染該癌別卡片

13. **【V3.2】PDF 表格被切成條狀資料**  → 9.69 手工整理成文字描述

14. **【V3.2】Header 雙色配色視覺亂**  → 改純單色

15. **【V3.3】is_empty_parent 判斷錯誤**  → 改用「有沒有編號條文 1.」判斷

16. **【V3.3】父項目逐癌切片粗暴歸類**  → 對父項目合併後的每個 clause 再做一次精細匹配

17. **【V3.3】空父項目展開時無內容**  → 加入「父分類提示卡」+ 來源標記

18. **【V3.4】健保署網站擋台灣 IP 爬蟲(包括 GitHub Actions IP)**
    - 症狀:容器 + GitHub Actions runner 4 個 UA 全部 HTTP 403
    - 做法:**多重備援策略**(直連 + r.jina.ai + Google Cache + Wayback Machine)
    - 實測 2026/5/1:GitHub Actions 第二次手動 trigger 時直連成功 (HTTP 200,117KB),猜測健保署擋人有時間/隨機性
    - 重要結論:即使第一次成功,後備策略仍要保留(下次可能換 IP 就被擋)

19. **【V3.5】要自動下載 PDF 給人類重做**
    - 需求:Issue 通知時直接附 PDF 下載連結
    - 限制:GitHub Actions API 建 Issue 時無法直接附檔
    - 做法:把 PDF 上傳為 artifact (90 天保留),Issue body 內貼 artifact 下載連結
    - PDF URL 抓取:從第九節到第十節之間找 `chap9_NNNNNNN.pdf` 或 `dl-XXXXX-...pdf` 連結
    - 抓不到時會在 Issue 內提供原始健保署連結讓人手動下載

---

20. **【V3.5】Header 顯示版本號和資料截止日**
    - 需求:Sela 想知道目前看的是哪個版本、資料是哪天的
    - 做法:topbar 加 `<span class="brand-version">v3.5</span>` 和 `<span class="brand-data-date">資料截止 115/4/23</span>`
    - 手機版 (≤768px) 把資料截止日獨立成第二行避免擠
    - 超小螢幕 (≤380px) 隱藏版本號

21. **【V3.5】小更新時 Claude 接力的處理流程**
    - 需求:之後 Sela 只會給第九節 PDF (不再是整本 410 頁),Claude 怎麼快速處理
    - 做法:寫了 `.github/未來更新流程設計.md` 詳細列出 9 步驟
    - 關鍵:不要重做 UI,只改資料;產出 DIFF 報告讓 Sela 確認

22. **【V3.6】無編號短條文藥物被當「無內容」**
    - 症狀:9.6、9.8、9.13、9.15、9.16.2、9.23、9.33、9.40、9.5.2 等 9 個藥物展開後沒內容
    - 原因:這些藥物的條文是直接寫在藥名行之後,沒有 1./2./3. 編號 (例如 9.6 「限惡性腫瘤患者患有惡性腹水...」)
    - parser 只認編號條文 → 整段被忽略
    - 做法:parser 改成「先找第一個編號條文 1.」,有就從這開始; 沒有就把藥名行之後當單一條文 (標 `_is_unnumbered`)
    - 結果:從 10 個無內容降到 0 個

23. **【V3.6】胰臟神經內分泌瘤等癌別衝突**
    - 症狀:9.31 Sunitinib「胰臟神經內分泌瘤」被同時歸類為「肝膽胰胃癌」+「其他癌」(切片外溢)
    - 原因:keyword 「胰臟」「胰腺」會優先匹配「肝膽胰胃癌」,但臨床上應該歸 NET (其他癌)
    - 做法:加排除規則 — 條文前 50 字含「神經內分泌」「間質腫瘤」「GIST」時,跳過「肝膽胰胃癌」「大腸直腸癌」分類
    - 同樣處理:腸胃道間質瘤 (GIST) 不該歸大腸或胃癌

24. **【V3.6】父項目 cancers 沒包含子項目所有 cancers**
    - 症狀:9.5 Paclitaxel 父項目 cancers=[乳/肺/婦/其他],但 9.5.2 子項目是肝膽胰胃癌 → 父項目切片外溢到肝膽胰胃癌
    - 原因:父項目 cancers 從原始解析 (繼承父表面文字),沒包含子的擴充
    - 做法:後處理階段 — 父項目的 cancers 加上所有子項目 cancers 的 union
    - 結果:外溢從 3 個降到 0 個

25. **【V3.6】Header 警示「有新版」機制**
    - 需求:當 GitHub Actions 偵測到健保署有新版時,前端要顯示警示
    - 做法:JS unlock 後呼叫 GitHub API
      `GET /repos/{owner}/{repo}/issues?labels=nhi-update&state=open`
      有未關閉的 nhi-update issue → 從 title 抓日期 → 跟 brand-data-date 比對 → 不一樣顯示橘色警示徽章
    - 從 location.hostname 推斷 GitHub repo (限 GitHub Pages 環境)
    - 失敗時靜默,不影響使用

26. **【V3.7】教學資料按鈕的事件冒泡問題**
    - 症狀:點教學資料按鈕反而觸發了藥物展開/收合
    - 原因:藥物 header 整塊綁了 click handler,按鈕在 header 內按下會冒泡
    - 做法:用事件委派(document level)+ stopPropagation() 阻止冒泡

27. **【V3.7】教學資料 JS 沒包 <script> 標籤**
    - 症狀:openEducation is not defined
    - 原因:程式插入 HTML 時忘了在新區塊外圍加 <script> 標籤,造成 JS 變成 HTML body 文字
    - 做法:確保插入位置前後是 </script> + <script> 配對,或自己包

28. **【V3.10】Header 加「最後檢查時間」確認爬蟲沒掛**
    - 需求:目前只有「有新版」才會跳通知,沒新版時 Sela 無法確認 workflow 是否還在跑
    - 做法:打 GitHub API `/actions/workflows/check-nhi-update.yml/runs?per_page=1` 抓最後一次 run 的 `created_at`
    - 顯示策略: 剛剛 / 今天 / 昨天 / N 天前 / 30 天以上顯示日期
    - 警示門檻:超過 14 天沒跑顯示橘色 (workflow 排程是每週一)
    - hover tooltip 補完整時間 + workflow 結果 (成功/失敗/⋯)
    - 順便把資料日期文字從「資料截止 115/4/23」改成「程式參考的資料是 115年4月23日」
    - 重點:`brand-data-date` 加 `data-date="115.4.23"` 屬性,讓 `checkUpdateNotice` 比對日期時讀屬性而非顯示文字 — 之後改顯示格式不會打到 regex

29. **【V3.11】PDF parser 漏抓的「歷史藥」直到健保署更新才被發現**
    - 症狀:做 115/5/22 更新差異報告時發現「新增」5 個藥(9.49/9.84/9.99/9.135/9.136),但其中 9.49 Abiraterone (條文最新日 114/6/1)、9.99 Gilteritinib (條文最新日 113/6/1)、9.84 Copanlisib (這次標"刪除")實際在 115/4/23 規定本來就有,是 V3.9 解析時就漏抓
    - 連帶:9.48 (Eribulin) 的 items / per_cancer["其他癌"] / search_text 都吃進了 9.49 整段內容; 9.98 (Pemigatinib) 的最後 item.header 吃進 9.99 字首
    - 原因:parser 沒設「下一個 9.X. 編號就是切點」這個邊界,造成漏抓的編號條文被合併到前一個藥
    - 做法:這次手工清理 — 把 9.48 items 從 7 個截回 2 個 (轉移性乳癌 + 脂肪肉瘤);per_cancer["其他癌"] / common_data / search_text 也都重建; 補加 9.49 / 9.99 / 9.135 / 9.136 完整條文;9.84 標 `deleted: true` 保留編號 (灰底淡出 + 紅色"已刪除 115/6/1"標籤,健保署也是用"(刪除)"寫法保留條文編號)
    - 教訓:下次重跑 parser 時應加上「9.X. 編號 + 英文藥名」邊界檢測,任一藥的條文若包含其他 9.X. 編號 = parser bug,該報錯而非靜默合併

30. **【V4.0.0 兩層 diff：法規更新有「編號層」+「條文層」兩個層級】**
    - 症狀：V3.11 跟進 115/5/22 更新時只比對「9.X 編號出現/消失」，漏掉 10 個既有編號的「內部條文修訂」，Sela 用 9.69 PD-L1 截圖才撞到 P016 術前輔助 nivolumab(115/6/1)完全缺漏
    - 原因：diff 方法論只做編號層，沒做條文層
    - 做法：建立兩層 diff 引擎(diff_engine.py)——(1)編號層比對新增/刪除；(2)條文層把每個 9.X 段落忽略空白後逐條比對，標出所有含 (115/6/1) 的新條文。實測除驗證 V3.11 的 10 個外，**多抓到 9.51 Regorafenib**(V3.11 名單外)、**排除 9.71 Venetoclax**(純 OCR 雜訊:R-CHOP 連字號被讀成控制字元 \\u0002)
    - 教訓：每次更新前一定先跑兩層 diff，產 DIFF 報告給 Sela 確認再重建。掃描全 PDF 找 `(11X/X/X)` 標記是抓條文層變動的關鍵

31. **【V4.0.0 search_text 沒跟著 items 重生 → 搜尋索引與顯示不同步】**
    - 症狀：寬驗證(逐藥比對 App vs PDF)發現 9.49/9.99/9.135/9.136/9.84 的 search_text 是 null;9.98 Pemigatinib 的 search_text 整段塞著 9.99 Gilteritinib 條文(items 在 V3.11 已清,search_text 沒清)
    - 原因：V3.11 手工改了 items 但沒重生 search_text。(註:有 runtime fallback `if(!d.search_text)` 從 items 補,所以 5 個 null 的其實搜得到;9.98 因非 null 不觸發 fallback,溢出是真的)
    - 做法：**全庫 149 個藥 search_text 一律從 items 重新產生**(format = `lower(name+brand+每個 item header+每個 subitem text)`,用 143/149 逐字回歸驗證格式正確);並把跨癌藥的 per_cancer 切片內容也納入 search_text,讓 P016/術前輔助等可被查到
    - 教訓：任何改 items/per_cancer 後,**一律重生 search_text**,別靠 runtime fallback

32. **【V4.0.0 前言適應症句被 parser 吃掉】**
    - 症狀：寬驗證發現 9.68 Radium-223(整句「限治去勢抗性攝護腺癌…」)、9.19 Estramustine(「限晚期前列腺癌…」)、9.56 Brentuximab(「限用於成人患者」)的「前言」不見了
    - 原因：條文在藥名行和第「1.」項之間的引言,V3.9 parser 只處理「完全無編號」(坑 #22)的情形,沒處理「有前言又有編號項」的混合情形,前言被丟棄
    - 做法：把前言補成第一個 `_is_unnumbered` item,放在編號項之前
    - 教訓：parser 要區分三種：純編號 / 純無編號 / 前言+編號混合

33. **【V4.1.1 篩選選項藏在摺疊線以下 = 等於沒有】**
    - 症狀：「只顯示需事前審查」開關放在篩選面板最後一節，手機上癌別(10 pills)+藥物類型把它擠到視窗外，使用者回報「要先勾別的才會出現」。
    - 原因：靠捲動才看得到的選項，在手機上等於隱形；且它是全域開關，不該排在兩大 pill 區之後。
    - 做法：把它移到面板最上面（癌別之前），開啟面板即見。
    - 教訓：手機面板上「全域開關/重要選項」要放在不需捲動就看得到的位置；別假設使用者會捲到底。

34. **【V4.1.2 flexbox 面板捲不動 = min-height:0 經典坑】**
    - 症狀：篩選面板下半（藥物類型下半 + 底部按鈕）看不到，電腦滑鼠滾輪也滾不動。
    - 原因：`.filter-panel` 是 `display:flex;flex-direction:column;overflow-y:auto` + 100vh；`.filter-body` 是 `flex:1` 但**沒有** `overflow-y` 也**沒有** `min-height:0`。flex 子項預設 `min-height:auto`（不會小於內容），於是 header+body+footer 剛好等於 100vh、面板永遠不溢出 → 不出捲軸；body 自己又沒開捲動 → 內容被裁、捲不動。
    - 做法：scroll 容器要落在 body：`.filter-body{overflow-y:auto;min-height:0;-webkit-overflow-scrolling:touch}`，面板改 `overflow:hidden`。(同一條 CSS 也修好「關於」面板。)
    - 教訓：**flex column 裡要捲動的子項，一定要 `min-height:0` + `overflow-y:auto`**；只在外層 flex 容器設 overflow 不會生效。V4.1.1 移位置只是遮掩症狀，沒治本。

35. **【V4.2.1 跨段落 OCR 滲入：per_cancer 殘留 + search_text 復現】**
    - 症狀：9.98 Pemigatinib(膽管癌)被列在「血液淋巴癌」，且內容混進 9.99 Gilteritinib(白血病藥)。
    - 原因：OCR 讓 9.98 最後一條「每日限處方1粒。」吃進下一段「9.99.Gilteritinib(如 Xospata)：…」整塊。items 在 V3.11 已清，但 **per_cancer["血液淋巴癌"] 切片殘留**該滲入，slicer 還因此把 9.98 誤分到血液淋巴癌。V3.x 時 search_text 不含 per_cancer 所以看不出來；**V4.0.0 起 search_text 納入 per_cancer → 滲入內容復現**(也是 search_text 又出現 gilteritinib 的真正原因)。
    - 做法（三層）：
      (a) 母程式內嵌資料：移除 9.98 的 `per_cancer["血液淋巴癌"]` + 從 cancers 拿掉「血液淋巴癌」+ 重生 search_text → 本版完全乾淨。
      (b) **載入時 de-bleed 守門員**(index.html，drugs 陣列後)：對每藥的 items header / per_cancer clause 截斷第一個「外來 9.X. + 拉丁藥名」標記之後的內容、清空的切片連同 cancers 一起移除、被動過的藥重生 search_text。→ 即使未來 dev 端重生又帶髒，部署端載入即自我清理(危險的跨藥內容不會顯示/被搜到)。守門員**刻意不**自動拔掉癌別分類(怕誤刪正當分類)，故分類正確性仍靠 (a)/(c)。
      (c) **永久解(dev 端，須 Sela 處理)**：`drugs_final.json` 把 9.98 來源條文的 9.99 滲入剪掉、且確保 9.98 不被指派到血液淋巴癌；`make_html_v6.py` slicer 切片時加邊界偵測(任一 clause 含其他 9.X. 段落編號→報錯/截斷，呼應坑 #29)。源頭修好後重生才不會再帶髒。
    - 教訓：**衍生索引(search_text)的來源欄位若擴大(納入 per_cancer)，等於把舊有的殘留髒資料重新曝光**；清資料要清到所有來源欄位，不能只清 items。

36. **【V4.3.0 部署端更新的 per_cancer 天花板】**
    - 情境：115/8/1 大更新(15 藥)只能從已部署 index.html 改(無 dev 端 make_html_v6.py / drugs_final.json)。
    - 能可靠做：用 parser 從乾淨新文字重建每藥 items(預設檢視/搜尋正確)、加新藥、更新日期。
    - **做不到(可靠地)**：per_cancer 切片是 slicer 語意切出來的——有「whole-item 切」(clause num=item num，可依 num 重建內容，如 9.2/9.18)、也有「subitem 切」(item 2 的 (1)乳癌/(2)肺癌 被拆成各癌 clause，如 9.3/9.5.1)與「manual 切」(9.69 的 subtitles/pdl1 手工結構)。後兩者無法用 num 對應自動重生;且即使 whole-item，新增的 item 也不會自動進舊切片。
    - 教訓：**含 per_cancer 的法規更新，正解是 dev 端改 drugs_final.json + 重跑 make_html_v6.py**(items 與 per_cancer 一起重生)。部署端只能保證預設檢視/搜尋正確;逐癌篩選對 9.3/9.5.1/9.69 及各藥新增組合會落後，須 dev 端重切。

37. **【V5.0.0 資料模型改造：per_cancer 切片 → cancerTags 條文標籤】**
    - 背景：V3.2 起「選癌別」是靠 slicer 另存一份 `per_cancer[癌別].clauses` 切片，前端選癌別時**只渲染切片**。切片一旦漏條，畫面就藏條——而且藏得無聲無息（坑 #36 已記錄部署端無法可靠重生切片）。已知漏條：9.2 第8款(肺癌)、第9款(婦癌 EC)、9.5.1 婦癌 EC、9.69 婦癌。
    - 做法：改成**每條 items 貼 `cancerTags`**，前端篩的是原文本身：
      ```js
      const shown = d.items.filter(it => tagHit(it.cancerTags, sel));
      // tagHit: 無標籤 → 顯示；含 "*" → 顯示；與所選癌別交集 → 顯示
      ```
      規則：條文指明癌別 → 標該癌別（可多個）；通用條件／事審／療程上限／子藥物標題（`【9.x.y …】`）→ 標 `["*"]`（所有癌別都顯示）；反推對不到的條 → 也落 `["*"]`。**per_cancer / common_data / is_manual_split 三個欄位一併移除**（common_data 與 `["*"]` 條文完全重複），檔案由 1.24 MB 降到 1.06 MB。
    - **失敗模式翻轉（本次改造的真正目的）**：舊模型出錯 = 條文被藏；新模型出錯 = 頂多多顯示幾條。前者臨床上會漏給付條件，後者只是多讀兩行。**任何時候寧可多顯示。**
    - 子項級標籤：`subitems` 也可帶 `cancerTags`，但**只在「該 item 的每個子項都成功標到癌別」時才啟用**（否則整條全顯示），且被篩掉的子項不是消失，而是收進「本條另有其他癌別條文 N 項」的摺疊區——所以子項級篩選也不會真的藏條。目前 38 個子項有標籤（9.3 item2、9.5.1 item2、9.69 item1/2 等「一條混多癌」的條文）。
    - 9.69 特案：per_cancer 是 `subtitles/single_use/combo_use/pdl1` 手工結構，不是 clauses。改用**病名前綴對映表**（`MAP_969`）逐子項打標，21 個子項全數命中；順帶補回舊切片漏掉的**婦癌**（item2 (10) 子宮內膜癌 dostarlimab）。選癌別後 9.69 從 32 條降到 12–16 條。
    - 驗證方法（無瀏覽器環境時的替代）：①「內容涵蓋度」——舊切片每條文字都必須仍出現在新篩選結果裡（151 藥只剩 1 筆未涵蓋，且是舊切片過期，見坑 #38）；② 條數對照（新 ≥ 舊，少數「多條併入同一 item」屬正常）；③ 逐癌別模擬篩選確認無「0 條」的藥；④ `node --check` 全部 script 區塊。
    - 教訓：**衍生資料（切片）與原文並存時，只要前端讀的是衍生資料，原文的更新就永遠可能追不上。正解是讓前端永遠讀原文，衍生資料退化成「原文上的標籤」。**

38. **【V5.0.0 反推時撿到的兩個既有資料 bug】**
    - **(a) 9.48 Eribulin 混進 9.49 Abiraterone 的前列腺癌條文**：`per_cancer["泌尿癌"]` 有第3、5款前列腺癌條文，但 9.48 的 `items` 只有「轉移性乳癌」「脂肪肉瘤」兩條——比對健保署原文確認 Eribulin 根本沒有前列腺癌適應症，是切片時吃到下一段 9.49。連帶 `cancers` 被誤加「泌尿癌」、`search_text` 也被污染。這是坑 #35 同型的跨段落滲入，但**只存在於 per_cancer，items 是乾淨的**——所以 de-bleed 守門員（只看「9.X. + 拉丁藥名」標記）抓不到。V5.0.0 已移除該切片與癌別；search_text 由 items 重生後污染自動消失。
    - **(b) `items[].num` 在同一藥內會重複**：9.12（9.12.1/9.12.2）、9.36（9.36.1/9.36.2）這類「父項合併子藥物」的藥，兩段各自從 1 編號。第一版反推腳本用 `num` 當 key，導致兩段標籤互相污染（9.12.2 的胰腺癌條文被標成大腸直腸癌）。**改用陣列 index 當 key 才正確。**
    - **(c) 9.66 的切片是過期的**：切片寫「不得與 regorafenib 併用」，items 已更新為「不得與 regorafenib **或 fruquintinib** 併用」。這正是坑 #36 說的「部署端更新只能更新 items、切片會落後」的實證——也再次說明切片這層本來就該拿掉。
    - 教訓：**同一份事實存兩份（items + per_cancer），久了一定會不一致；不一致時你不會知道畫面上讀到的是哪一份。**

39. **【V5.1.0 cancerTags 全庫重推：舊切片不能當真值來源】**
    - 症狀（外部審稿發現）：V5.0.0 的標籤是從舊 `per_cancer` 切片「反推」出來的，切片本身的漏條與錯誤原封不動被繼承。實例：9.2 第3款(轉移性鱗狀 NSCLC)被標成乳癌＋肺癌、第6款(早期三陰性乳癌)/第8款(可切除 NSCLC)/第9款(dMMR 子宮內膜癌)全落 `["*"]`；9.10 胰臟癌與食道癌條文落 `["*"]`、胃癌條文多帶食道癌；9.16 子宮頸癌條文帶到肺癌；9.5/9.5.1/9.9/9.95 的「一條混多癌」沒有子項標籤。
    - 根因兩個：(a) 反推以切片為唯一依據，切片漏的就只能落 `["*"]`；(b) 比對指紋用「正規化後前 18 字」，9.2 第3款與第6款都以「與 pembrolizumab 及 paclitaxel 併用於」開頭 → 前 18 字相同 → 誤命中。**固定長度前綴不能當條文指紋**，要用完整正規化文字的 hash（本版改用 `semanticHash`／`exactHash`：前者去掉修訂日期比對「是否同一條規定」，後者保留日期比對「是否逐字一致」）。
    - 做法：**標籤改由原文重新推導**，舊切片只用來產差異報告。固定推導優先序：
      1. **行政共通條文 → `["*"]`**（`universal-rule`）：使用條件／事前審查／登錄／申請／檢附／療效評估／停藥／給付時程／劑量上限／每位病人／病人之…等開頭。
      2. **病名辨識 → 對應癌別**（`text-rule`）：同義詞表（胰臟癌＝胰腺癌、腎細胞癌＝腎癌、泌尿道上皮癌＝尿路上皮癌、乳癌＝乳腺癌、NSCLC／SCLC／HCC／GIST…），**長詞優先並逐一消耗**，所以「胃食道接合處」不會被當成食道癌。
      3. **都標不到 → `["*"]`**（`fallback-unclassified`）。畫面上與 1. 相同，但建置報告分流，否則真正未分類的條文會永遠躲在「通用」裡沒人清查。
    - **關鍵規則（比關鍵字表更重要）**：
      - **條文標題自己指明癌別 → 該條就是那個適應症，子項是它的附帶條件**（子項一律 `["*"]`，跟著父條顯示）。
      - **標題沒指明癌別（傘狀／行政條文，如「限給付於下列情形：」「前述1.之成人病人，包括之適應症有:」）→ 整條一律 `["*"]` 永遠顯示，篩選交給子項各自的標籤。**
      - 這條規則同時修掉「乳癌篩選看到肺癌術後條文」與「9.103 CAR-T 因為排除條件提到乳腺癌原位癌而被歸到乳癌」兩類錯誤。
    - **器官單詞是誤判來源**：`卵巢`／`子宮頸`／`睪丸`／`唾液腺`／`頭頸部`／`漿細胞` 這類要改成「須帶癌症字尾」的正則。實際踩到：9.1 芳香環酶抑制劑「限停經後或**卵巢切除**後」、9.72 CDK4/6「病患目前未接受**卵巢功能**抑制治療」都被誤標婦癌。
    - **排除語句要先剝掉**：「（不含胃腸基質瘤及神經內分泌腫瘤）」「除…以外」「排除…」內的病名不是適應症。9.69 胃癌條文原本因此多帶其他癌、頭頸癌條文多帶鼻咽癌。
    - 成果：藥物層 `cancers` 改為**由條文標籤聯集自動推導**（不再手維護），修掉 9.12.1 缺肝膽胰胃癌、9.13 缺其他癌(惡性黑色素癌)、9.14 缺其他癌(Kaposi sarcoma)、9.95 缺乳癌(分泌型乳腺癌)與頭頸癌(唾液腺腫瘤)、9.69 缺婦癌，並移除 9.48 誤掛的泌尿癌。
    - 教訓：**從壞資料反推出來的標籤，只會得到一份「格式正確的壞資料」。要重建語意，來源必須是原文，不是上一版的加工品。**

40. **【V5.1.0 原文轉換的內容損失：截斷日期與上標數字】**
    - 症狀：44 個 header／subitem 以「、」結尾（修訂日期被截斷，如 9.11 只剩 `(89/10/1、`）；另有 8 處句中日期括號未閉合；`mm3`／`mg/m2`／`1.73m2`／`10⁻³` 的指數在早期 parser 掉了（`lymphocyte/mm `、`每日最大劑量200mg/m `、`敏感度需達10 (0.1%)`）——後者已不是排版問題，是**有臨床意義的數值**。
    - 做法：健保署來源檔（`chap9_*.pdf` 實為純文字）建「去空白 flat 索引 + 回原檔 offset 映射」，用條文尾端指紋定位後把完整日期括號讀回來。三個必要技巧：(a) 指紋長度要可退讓（200→20 字），(b) **允許多處命中，但所有命中推出的續尾必須一致才採用**（父子項目重複條文會多處命中），(c) 前向定位失敗時改用**括號之後的文字反向定位**（9.85 兩條「須經事前審查核准後使用：」開頭相同，只能靠後文區分）。頁碼雜訊 `第9節-\d+` 要先剝掉。上標一律存 **Unicode `m²`／`mm³`／`10⁻³`**——`normSearch` 有做 NFKC，`²` 會正規化成 `2`，所以打 `mg/m2` 或 `mg/m²` 都搜得到，不必靠 `<sup>` 保存數值。
    - **驗證要放在建置端，不是 runtime**：審稿建議在前端 `throw new Error` 攔截截斷條文——不可以，瀏覽器丟例外會讓個管師看到空白頁。改成 `validate_v51.py`：BLOCK 級（截斷／括號不平衡／單位缺指數／父子同條文標籤不一致／`cancers` 不等於標籤聯集／`pre_review` 父子不符／`"*"` 與癌別並存／出版檔與稽核檔標籤不符）任何一筆就不出版；WARN 級（含癌名卻判 `*`、單條命中 3+ 癌別、只靠單一模糊器官詞、`fallback-unclassified`、癌別大量增減）只產報告。目前 BLOCK 0 / WARN 284。
    - 教訓：**「資料合規」要有一支會讓建置失敗的腳本，不能只寫在文件裡靠自律。**

41. **【V5.2.0 被壓平的表格＝畫面雜亂的真正元凶（9.69 PD-L1／事審代碼對照表）】**
    - 症狀（Sela 指出）：9.69 免疫檢查點抑制劑第3款「病人之生物標記表現」在畫面上是一團 **3,901 字的純文字**。原文是一張 **29 個給付範圍 × 6 支藥** 的矩陣（含事審代碼 P001…P151、TPS／TC／IC／CPS 門檻），PDF 轉文字後行列全部黏成一段，而且「本藥品尚未給付於此適應症」出現 100 多次（6 欄裡通常只有 1–3 欄真的有給付）——雜訊佔了絕大多數版面。
    - 這也解釋了為什麼舊架構要手工做 `per_cancer[癌別].pdl1`：**不是因為切片是好設計，而是因為原文結構壞掉，只能人工摘要**。根治法是把表格結構補回來，不是另存摘要。
    - 做法：`parse_pdl1.py` 以**事審代碼 `P\d{3}` 當錨點**切列，代碼之後必須恰好吃滿 6 格；儲存格用語是封閉集合（`不需檢附報告`／`本藥品尚未給付於此適應症`／`TPS≧50%`／`TC≧50%或IC≧10%`／`CPS≧10`…共 20 種，長詞優先），逐格比對。跨頁重複 3 次的欄位標題列先移除、表尾註（`* Ventana SP263 僅適用於…`）另存。解析完 **round-trip 組回原字串必須逐字一致才寫出**（這是唯一能保證沒吃掉臨床數值的方法），且列數與格數進 `validate` 的 BLOCK 級檢查。
    - 資料結構：子項新增 `table: {columns[{drug,assay}], rows[{scope, code, cells[], cancerTags}], na, note}`。**每一列自己帶 `cancerTags`**，所以癌別篩選會篩到列層級：選肺癌 8 列、其他癌 3 列、乳癌／婦癌各 1 列（原本 29 列全出）。
    - 渲染：**只列出「真的有給付」的藥**（藥名 + PD-L1 門檻 + 對應 IVD 抗體/平台），尚未給付的收成一行「其餘 N 支藥品尚未給付於此適應症」；事審代碼做成 badge（個管師送審要填的就是它）。原表放進 `<details>` 可展開的橫向捲動表格，未給付格顯示 `—`，選了癌別時對應列標亮。
    - 順手做的：條文內的 `Ⅰ. Ⅱ. i. ii.` 層級標記原本全擠在同一行，改成換行縮排（**只認羅馬數字**，避免把「第1,4,8,11日給藥」誤斷）。9.18 早期乳癌那條 1,005 字的條文因此從一團變成 12 個可讀層級。
    - 教訓：**「內容雜亂」通常不是排版問題，是上游結構在轉檔時被壓平了。與其在前端想辦法排版，不如把結構解析回來——但一定要有 round-trip 驗證，否則等於重新輸入一份臨床資料。**

42. **【V5.3.0 全文掃一遍找「還有沒有被轉壞的表格」——沒有第二張，但找到三類其他遺失】**
    - 動機：既然 9.69 的表格被壓平沒人發現（坑 #41），必須確認全文還有沒有同類問題。
    - **表格偵測方法（可重複用）**：把原文切成 153 個 9.X 段，對每段算 (a) **重複片語**（長度 6–14、出現 ≥4 次的最長片語）與 (b) **短行比例**（≤12 字的行佔比）。矩陣被壓平的特徵是「同一個儲存格用語重複很多次 + 短行極多」。結果 **9.69 一枝獨秀**（「本藥品尚未給付於此適應症」重複 82 次、短行 66%），第二名只有 9 次且是藥名／日期的正常重複、短行 21%。**結論：全文只有 9.69 一張表格，沒有第二張被轉壞的。**
    - **但逐藥字元對比（原文 vs 我們的資料）抓到另外三類遺失**，方法是：把每個 9.X 段落與該藥所有 `header`／`subitem`／`table` 文字都去空白後比對，用 18 字滑動視窗找出「原文有、我們沒有」的連續區段，再把區段依「日期括號 / 條號 / 藥品標題行 / 其他」分類（不分類的話 90% 是誤報）。
      1. **藥品層前言／範圍限定被丟掉**（9 藥）。最重要的是 **9.20 Rituximab 的「用於抗癌瘤部分」**——這支藥同時有非癌症適應症（第 6 款 ANCA 血管炎），少了這句話會誤讀整節的適用範圍。其餘：9.10／9.12.1「（需符合藥品許可證登載之適應症）」、9.14「限用於下列適應症」、9.22／9.25／9.28／9.30「限用於」、9.84「(刪除)」。新增 `lead` 欄位並在藥品卡最上方以橘色 notice 呈現。
      2. **藥品層歷次修訂生效日完全沒存**（716 筆）。條文內的日期有存，但藥名後面那串「（91/4/1、93/8/1、…）」從來沒進資料。新增 `revisions` 欄位，以可摺疊的「健保署歷次修訂生效日（N 次）」呈現。
      3. 控制字元 **0 筆**（來源檔 9.18 有一個 `\x02`，我們的資料已乾淨）；其他實質條文遺失 **0 筆**。
    - 踩到的抽取陷阱：`lead` 會吃進第一條條文（9.30 Dasatinib 整條被當前言）→ 要在第一個「數字.」處截斷；會抓到藥名殘片（9.91 的 Trametinib、9.136 的 Fruquintinib）→ **合法前言一定含中文**，純拉丁字串一律丟掉；`revisions` 會抓到 `(1)(2)(3)` 這種條號 → 只收 `\d{2,3}/\d{1,2}/\d{1,2}`。三個都加進 `validate` 的 BLOCK 級（`lead` 必須逐字存在於健保署原文、`revisions` 必須符合民國日期格式）。
    - 教訓：**「有沒有其他地方也壞了」要用可量化的偵測指標去掃，不能靠印象；而逐字元對比一定要先把可解釋的差異（條號、標題行、日期、上標轉換）分類扣掉，否則報告裡 90% 是誤報，看兩眼就會放棄。**

43. **【V5.3.1 改成手風琴，順帶必須修掉教學按鈕的事件順序 bug】**
    - 需求：展開一支藥時把原本展開的收起來（一次只開一支）。
    - **為什麼順帶要修教學按鈕**：原本 `h.onclick = () => h.parentElement.classList.toggle("open")` 綁在 `.drug-header` 上，而教學按鈕靠 **document 層的冒泡階段** `stopPropagation()` 想阻止展開——但冒泡是先跑 target 上的 handler、再往上跑到 document，所以卡片早就被 toggle 過了，那句 `stopPropagation()` 從來沒生效（審稿列為 V5.1.1 第①項）。改成手風琴後這個 bug 會**變嚴重**：點教學資料不只會展開自己，還會把別人收起來。所以這兩件事必須一起做，在 header handler 裡用 `e.target.closest(".edu-btn, .cdq-actions, [data-fav], [data-copy], [data-ec]")` 直接排除。（常用／複製連結／副作用照護三個按鈕原本就沒事，因為它們用的是 **capture 階段** 的 document listener，比 header handler 早跑。）
    - **收起上方長條文會讓版面上移**，剛點開的標題可能被推出視野 → 展開後 `requestAnimationFrame` 裡做 `scrollIntoView({block:"nearest", behavior:"smooth"})`。
    - 教訓：**`stopPropagation()` 只能擋「還沒跑到的」handler。要擋元素自己的 handler，只有兩條路：用 capture 階段，或在那個 handler 裡自己排除。** 註解寫「防止觸發藥物展開」不等於真的防到了。

44. **【V5.4.0 對齊 SELA Kit V1.27.0：Kit 跳 18 版的落差評估】**
    - 情境：本專案上次對齊的是 Kit **V1.9.0**（V4.1.0 首次對齊），現在 Kit 已到 **V1.27.0**。依 `claude-init.md` 第二章 V1.25.0 新增情境（「已對齊過但 Kit 跳版、落差 ≥3 個 b 版」）走**只評估落差區間新規範**，不重跑首次對齊流程（會洗掉 43 條坑）。
    - **本專案的教訓已經回流成 Kit 級規範**：V5.1.0 的 handoff 被 Kit V1.27.0 收成 **坑 #83「衍生資料兩鐵則：UI 永遠讀原文、遷移輸入必用原始權威來源」**（與 OncoPA 併為 N=2 佐證），validate 的 BLOCK/WARN 分級也補進 Kit **坑 #7**。→ 本專案坑 #37–#39 從此有 Kit 級對應條目，跨專案共用。
    - 落差區間抓到的實際不合規（不是格式問題，是真的錯的）：
      - **「關於本系統」面板停在 V4.x 的世界觀**：還寫「逐癌切片狀態：9.69(手工)、其他自動切片／尚未切片：9.5、9.16、9.36」——V5.0.0 就沒有切片架構了；資料日期還寫「115/5/22 公告」——V4.2.0 起是 115/7/23。**這是坑 #3「資料與顯示必須同源」的變體：說明文字是第二份真相，改架構時沒人想到要改它。**教訓：**改架構時，「說明自己怎麼運作」的靜態文案必須進同步清單**，否則它會用舊架構的語言誤導使用者，而且比程式 bug 更難被發現（沒有任何測試會抓）。
      - `console.log` 2 處（catch 區塊的診斷輸出）違反 §3「不留 debug console.log」→ 改 `console.warn`（語意也更正確）。
      - `.gitignore` 的專案特定段列的是 **V4.x 時代不存在的檔案**（`drugs_final.json`、`per_cancer_data.*`、`make_html_v6.py`——V5.0.0 已確認沒有這套 dev pipeline），等於忽略清單在保護幽靈。→ 換成真實的建置端腳本與稽核檔。
    - **優化體檢（§V1.17.0 鐵律，含 V1.26.0 的技術棧前篩）**：OPT-1/2/3 為後端 / DB / 外部請求主題，純靜態單檔 HTML **技術上不適用**（前篩剔除 3 條）；實際對照 1 條——**OPT-4「生成後配自我驗證迴圈」本專案已實質落實**（`validate_v51.py` BLOCK/WARN 分級、PD-L1 表 round-trip 逐字比對、`lead` 必須存在於原文、151 藥×6 癌別實跑渲染、`node --check`）。→ **0 項可升級**，且這是「已檢查、確認不用改」的結論，不是沒檢查。
    - **App logo 雙軌（§8.3 鐵律：既有業務 app 首次對齊必須主動詢問）**：SELA 已要求產 logo prompt → 依 §17.4 產出 `SELA-logo-prompt.md`。同時**先把 SELA 歸屬印記建好**（「關於本系統」底部，引用**獨立的** `favicon/sela.svg`）——依 §9 V1.19.0 規則，favicon 日後換成 app logo 時，歸屬印記若重用 favicon 引用會一起變成 app logo、雙軌破功。**先建印記、後換 favicon** 的順序才安全。
    - **不做（明寫理由，依對齊 SOP 四級分類法的「✗ 不做」）**：
      - ✗ **不改主題色 `#436f8a` 成 Kit 預設北歐霧藍 `#5A7A8B`** — 已上線驗證的配色是事實標準，對齊 SOP 鐵律第 2 條明文禁止「為對齊 Kit 而改既有設計」。
      - ✗ **不改資料夾／zip 名稱** — `Cancer Drug` 已是英文，§10.5 對已英文化者不主動回頭改。
      - ✗ **不套 `templates/CLAUDE-template.md`** — 對齊 SOP 鐵律第 1 條，會洗掉 43 條坑。
      - ✗ **不改 `console.info`／`console.error`** — 那是 de-bleed 守門員的刻意輸出（坑 #35），不是 debug。
    - 教訓：**Kit 跳版的落差評估，價值不在補格式，而在「用一份新的檢核清單重看自己的專案」**。這次真正撿到的三件事（關於面板停在舊架構、.gitignore 保護幽靈檔、debug log）都不是 Kit 新規範才禁止的，是**舊規範一直有、但沒人回頭核對**。

45. **【V5.4.1 手機版面被撐開：又是 flex 沒 min-width:0（坑 #34 第二次咬）】**
    - 症狀（Sela 回報）：手機上格式跑掉，而且「自己打的星號會在字中間」。
    - **根因一（版面）**：`.subitem-text` 與 `.clause-text` 都是 `flex: 1` 但**沒有 `min-width: 0`**。V5.2.0 把 PD-L1 對照表放進子項，那張表是 `min-width: 720px` — flex 子項預設 `min-width: auto`（不小於內容寬），所以在 380px 手機上 `.subitem-text` **拒絕縮小**，整個 `.drug-body` 連頁面一起被撐開。`.pdl1-scroll` 雖然有 `overflow-x:auto`，但**外層不肯縮，內層的橫向捲動就永遠用不到**。
    - **這是坑 #34 的第二次踩**（V4.1.2 是篩選面板捲不動、同一個 `min-height/min-width: auto` 陷阱）。教訓：**在 flex 容器裡塞任何「有固定最小寬度」的東西（表格 / 程式碼區 / 圖片）之前，先確認那條軸上的 flex 子項有 `min-width: 0`。** 只在該元素上設 `overflow` 不會生效。
    - **根因二（星號）**：兩個來源都是半形 `*`——(a) 健保署 PD-L1 表的註記符號 `Ventana SP263*` 與表尾 `* Ventana SP263 僅適用於…`；(b) 9.103 的 `2*106 CD34+細胞`（原文用 `*` 當乘號、指數在上游轉檔就掉了，坑 #42 已記）。桌機一行讀得懂，**手機換行後 `*` 落在行首或字中間，看起來就像誤打的星號**。
      - 做法：`footnoteMark()` 把註記 `*` 轉成上標 `※`；`formatHierarchy()` 加一條 `(\d)\s*\*\s*10(\d)` → `$1×10<sup>$2</sup>`。**兩者都是純顯示轉換，底層資料仍與健保署原文逐字一致**（validate 的 round-trip 與「lead 必須存在於原文」檢查不會被破壞）。
      - 教訓：**半形 `*` 在中文段落裡是視覺地雷** — 使用者不會把它讀成註記符號，會讀成錯字。原文用 `*` 當註記或乘號時，顯示層一律換成 `※` 或 `×`。
    - **順手移除逐藥「副作用照護」按鈕**：那顆按鈕對 151 支藥送的是**同一個網址**（癌症希望基金會首頁，per-drug 深連結一直是未完成的候選工作），逐藥各放一顆只是佔標題列版面、在手機上還把按鈕擠成兩行。改成「關於本系統 → 相關資源」放一個入口。教訓：**「每個項目都有、但內容完全相同」的按鈕＝該收成全站單一入口。**

46. **【V5.4.2 「複製連結」對沒帳號的人沒用 → 改成一鍵複製條文】**
    - 需求來源（Sela）：**「複製連結不實用，因為不是每個人都有這程式權限」**。深連結要對方也能進系統才有意義，但個管師要傳條文的對象常是醫師、藥師、病房——他們沒有這支工具的密碼。
    - 教訓：**判斷一個分享功能好不好用，要看「接收端有沒有你的環境」**。深連結、匯出檔、系統內轉寄都預設對方在同一個系統裡；跨組織溝通的通用貨幣是**純文字**。
    - 做法：按鈕改成「複製條文」，產出可直接貼進 LINE／病歷／送審備註的純文字：
      - **跟著目前的癌別篩選**（選肺癌 → 9.69 只複製 8 條相關的、7,229 字；不選 → 全文），這樣複製到的就是畫面上看到的。
      - 標題含藥號＋學名＋商品名；`lead`（範圍限定）、〔通用〕標記、被篩掉的子項數（「本條另有其他癌別條文 N 項，未列入」）都保留——**讓接收端知道這是節錄、不是全文**。
      - PD-L1 對照表轉成逐列文字（只列有給付的藥＋門檻＋檢測平台＋事審代碼）。
      - 事前審查提示 + **自動附上資料公告／生效日期與出處**。文字一離開系統就沒有畫面上的資料日期了，這行是防止過期條文被當成現行規定的唯一線索。
    - **顯示層轉換要重用同一套規則**：純文字版一樣把 `*` 轉 `※`、`2*106` 轉 `2×10^6`、去掉 OCR 塞在中文字間的空白（坑 #45），否則貼出去的文字比畫面還醜。
    - **踩到的順序 bug**：`plainText()` 原本先插入羅馬數字層級的換行＋縮排、最後才 `replace(/[ \t]+/g," ")` 收斂空白——**那行把剛插入的縮排全吃掉了**，輸出變成每層都只有一個空格。正解：**收斂空白必須在插入縮排之前**。教訓：字串管線裡「正規化」與「加格式」有先後順序，正規化一律先做。
    - **複製要有後援**：`navigator.clipboard` 在舊 iOS Safari / 非 https 會失敗，且本功能主要在手機上用 → 加 textarea + `execCommand('copy')` 後援，兩層都失敗才提示手動長按選取。（舊的「複製連結」失敗時退回顯示 URL 讓人手抄，對 7,000 字的條文完全不可行。）
    - **跨 script 區塊取資料**：cdq 工具列在另一個 `<script>` 區塊，看不到主程式 IIFE 內的 `drugs`／`tagHit`／`selectedCancers` → 在主程式掛 `window.cdqClauseText` 當唯一出口。**不要為了取用資料把 IIFE 拆開**（會讓全域污染回來）。

47. **【V5.5.0 App logo 上線：Gemini 給的線稿在 16px 一定糊，而且主體只佔畫布 6%】**
    - 走 Kit logo §10.2 工作流 B（Sela 用 Gemini 生圖、Claude 優化轉檔）。原圖 2048×2048 RGBA、乾淨純白底、無假棋盤格。
    - **踩到的第一件事：連通區域才分得出「外白底」與「主體白色」。** 直接對四角 `floodfill(thresh=40)` 之後，圓角方形邊界仍留一圈抗鋸齒淺色像素，用「亮度 > 門檻」抓主體 bbox 會抓到**整個圓角方形**（bbox 佔畫布 80%），裁切完全沒效果。正解：對「是白色」的二值遮罩做 `ndimage.label`，**與影像邊界相連的白色區塊 = 外底**，其餘白色 = 主體；外底再 `binary_dilation` 6px 吃掉抗鋸齒帶後才塗底色。
    - **踩到的第二件事：主體只佔畫布 6%（白色像素比）。** 生圖 AI 在圓角方形內留了很大的留白，直接縮成 16px 幾乎是一塊純色。依 §10.2 步驟 4「規整比例」重新裁切成主體佔 82%、四周 9% 安全邊界後，白色佔比 6% → 16.5%。**這步比任何後處理都有效**，但 prompt 裡寫「小尺寸也要清楚」完全沒用（§10.4 已明載生圖 AI 沒有 optical sizing 概念）。
    - **量化小尺寸可辨識度，不要靠肉眼**（呼應坑 #42 的「用可量化指標掃」）：對每個尺寸算「右半水平中線的明暗交替次數」——同心圓靶心若還在，交替次數 > 0。結果：完整版 16/24/32/48px 全部 **0 次**（靶心整團糊掉）、64px 才開始出現 2 次。這就是 §10.4「兩套母圖」的觸發條件。
    - **簡化實心剪影版**（§10.4 + §10.3 簡單幾何範圍）：同一組幾何常數（膠囊 bbox、靶心圓心）改畫成「實心白膠囊 + 右半負空間圓環 + 中心點」，4× 超取樣後縮圖。16px 交替 **4 次**、灰糊比例 16%。**兩套共用同一組常數產生 → 底色、主體、氣質必然一致**（§10.4 第 4 條要求），不會變成兩個 logo。
    - 配置：**≤48px（favicon-16/32、favicon.ico 內嵌的 16/32/48、SVG favicon）用簡化版；≥64px（apple-touch-icon 180、android-chrome 192/512、入口頁 256）用完整版。**
    - **底色正規化**：Gemini 給的是 `#486D87`（≈ 但不等於 prompt 指定的 `#436f8a`）。用「亮度混合係數」重算每個像素（`t=(lum-lum_bg)/(255-lum_bg)`，輸出 `底色×(1-t)+白×t`），底色被校正成精確值、同時保留主體邊緣抗鋸齒。**不要用「顏色相近就整片換色」**，那會把抗鋸齒邊緣鋸掉。
    - **雙軌落地（§9）**：favicon 全套、入口頁、頂部工具列 → app logo；SELA logo **只留在「關於本系統」底部歸屬印記**，且引用的是獨立的 `favicon/sela.svg`（V5.4.0 就先建好，所以這版換 favicon 沒有破功）。`<link rel="icon" type="image/svg+xml">` 一併改指 `app-logo.svg`——若還指 SELA，現代瀏覽器優先吃 SVG 會出現「分頁是 SELA、桌面捷徑是 app logo」的不一致（§4.1 V1.19.0）。
    - 順帶：兩處 base64 內嵌 JPEG（37 KB）換成 `favicon/` 檔案引用，`index.html` 反而小了 37 KB。

    - **V5.5.1 修正（Sela 回報「簡化版少了原來的雷達意境」）**：V5.5.0 的簡化版只留「實心膠囊＋單一圓環＋中心點」，把靶心的雷達感做掉了。兩個原因：
      1. **簡化過頭**：§10.4 說「拿掉外框與內部細節、只留主體輪廓 + 一個負空間特徵」——我照字面只留一個環，但**雷達的識別特徵不是環的數量，是「十字刻度線穿出圓環」**。改成：實心白膠囊上打**負空間三層**（底色外環→白環→底色中心點）＋**白色十字線畫在最上層**（把環切成弧、並在膠囊上下露出刻度）。同一組原圖量測常數（膠囊 93..932×325..700、靶心 706,512、環半徑 210/150/90、十字線半寬 50）。
      2. **量測指標選錯，害我把分界訂太保守**：V5.5.0 用「右半**水平中線**的明暗交替次數」——但**水平十字線正好躺在中線上，整條都是白的**，所以永遠量到 0，誤判「完整版 48px 也糊」。改用**避開十字線的 35° 對角掃描**後真相是：完整版的環在 **16–24px 為 0 條、28–32px 剩 1 條、48px 起 7 條完全清楚**。→ 分界從「≤48 簡化」修正為 **「≤32px 用簡化版、≥48px 用完整版」**，`favicon.ico` 內嵌 16/32 用簡化版、48px 由 `app-logo-48.png`（完整版）提供。
      - 教訓：**量化指標本身要先驗證會不會被自己的圖形結構騙到。** 掃描線剛好與主體的一條直線重合，指標就永遠讀不到變化——這跟坑 #42「用可量化指標掃」是同一組工具，但指標選錯比沒量更糟：它給你一個看起來很硬的錯誤答案。
    - 教訓：**「AI 生成的 logo 拿來就能用」幾乎不成立。** 至少要過三關：連通區域去背、重新裁切安全邊界、小尺寸量化檢查決定要不要做第二套母圖。

48. **【V5.5.2 雙 logo 制落地：三個位置 + 一個只有 viewBox 的 SVG 差點爆版】**
    - 需求（Sela）：**「這程式應該走雙 logo 制，我的 logo 找個地方放上」**。V5.5.0 換上 app logo 後，SELA logo 只剩「關於本系統」面板底部——藏太深。
    - 依 Kit logo §9 共存規則放三處，**app logo 永遠主視覺、SELA 永遠輔助**：
      | 位置 | 放什麼 | 依據 |
      |---|---|---|
      | 入口密碼頁（＝ splash）| 上方 app logo 64px 主視覺、卡片最底 `by SELA` + 18px SELA logo（隔一條分隔線、相距約 300px）| §9「應用啟動畫面：app logo + 底部小字 by SELA」|
      | 主畫面頁尾 | 左「健保癌症藥物速查系統｜彰濱秀傳癌症中心」、右 `by SELA` + 16px | §9「文件 footer / 版權聲明 → SELA」|
      | 關於本系統底部 | 歸屬區 SELA logo（V5.4.0 已建）| §9「About 頁：底部歸屬區放 SELA logo」|
    - 三處**都引用獨立的 `favicon/sela.svg`**，不重用 app logo 的引用（§9 V1.19.0）。尺寸比 64:18 ＝ 3.6:1，且垂直分離遠超「一個段落」——不會出現 §9 禁止的「並排同尺寸」。配色不撞（SELA 橘 `#F36825` vs app 霧藍 `#436f8a`），§9 的同色隔離規則不觸發。
    - **列印時頁尾隱藏**（`@media print`）：印出來的是給醫師／病歷的條文，不該夾品牌標記。
    - **真正的坑：`sela.svg` 只有 `viewBox`、沒有 `width`/`height` 屬性。** 我原本只在 HTML 寫 `<img width="18" height="18">`，CSS 只有 `display:block`。用 WeasyPrint 渲染驗證時，那顆 18px 的印記**實際渲染成約 200px**，把整張入口卡片撐爆。
      - 原因：無固有尺寸的 SVG，`<img>` 的 HTML width/height 屬性在瀏覽器裡是權威的，但**任何 CSS 覆蓋、或非瀏覽器渲染器（列印引擎、PDF 產生器、郵件客戶端）都可能改用「填滿容器」的推算**。
      - 做法：**一律在 CSS 明確給 `width`/`height`**（`.sela-credit img{width:16px;height:16px;flex-shrink:0}`），不要只依賴 HTML 屬性。修完量測回到 17.7 CSS px。
      - 教訓：**引用第三方或既有的 SVG 之前，先看它有沒有固有尺寸**（`grep 'width=' *.svg`）。只有 viewBox 的 SVG 是「橡皮圖」，你不鎖它，它就會鎖你的版面。這也是為什麼這次要用渲染器實測而不是只看程式碼——**尺寸類的 bug 只有渲染出來才看得到**。

49. **【V5.5.3 藥名拼錯了三個版本沒人發現：`Fruquitinib` 少一個 n】**
    - 症狀（Sela 回報）：9.136 的藥名拼錯。健保署原文是 **`Fruquintinib`（如 Fruzaqla）**，我們的 `name` 是 `Fruquitinib`——**少一個 n**，從 V3.11 新增這支藥起錯到現在。
    - 為什麼躺這麼久：
      1. **錯字只差一個字母、又在拉丁藥名中間**，中文使用者掃過去不會停。
      2. **同一份資料裡有正確拼法可以對照，但沒人比對**：9.66 的條文寫「本藥品不得與 regorafenib 或 **fruquintinib** 併用」是對的——**全庫同時存在兩種拼法**，這種內部矛盾本來就該被自動抓出來。
      3. **前幾版的驗證只查條文，沒查藥名**。`validate` 已有「`lead` 必須逐字存在於原文」的 BLOCK 檢查（坑 #42），但 `name`／`brand` 從來不在檢查範圍——**它們被當成「metadata」而不是「從原文抽出來的資料」**，這個分類錯誤就是死角。
    - 做法：
      1. 修正 `name` 並重生該藥 `search_text`（舊拼法會讓「fruquintinib」搜不到這支藥——**這是實際的臨床查詢失敗**，不只是美觀問題）。
      2. **`validate` 新增 BLOCK 10：每支藥的 `name`／`brand`（以「、；/」拆開後各段）必須逐字出現在健保署原文中。** 立刻用它掃全庫 151 支 —— **只有 9.136 一支不合格**，其餘乾淨。
    - 教訓：**凡是「從權威來源抄出來的欄位」都要能溯源，包括你以為是 metadata 的欄位。** 判準不是「這欄位重不重要」，而是「**它的值有沒有一個外部權威來源？有 → 就要能自動比對**」。藥名、商品名、附表編號、事審代碼全屬此類。
    - 附帶：`grep` 全庫找同一實體的多種拼法（本案 `[Ff]ruq\w*` 得到三種：`fruquintinib` / `fruquitinib` / `Fruquitinib`）是最便宜的自查——**同一份資料裡出現同一個東西的兩種寫法，其中一種一定是錯的。**

50. **【V5.6.0 外部審稿 6 項核查：4 項屬實照改、2 項歸因錯誤但點出真問題】**
    - 這次審稿的價值不只在「找到錯」，在於**兩項它歸因錯的，反而暴露了原文本身的問題**。逐項核查結果：

    | 審稿項目 | 核查結果 | 處理 |
    |---|---|---|
    | ① 9.69 術前輔助漏 `Alimta Avos` | **原文就沒有** | 不改條文，加官方交叉差異註記 |
    | ② P151 六欄全「尚未給付」寫法不對 | **我們與原文一致**，但判讀風險屬實 | 加列註記 |
    | ③ 9.55 / 9.114 缺指數 9 | **屬實** | 修正 + 顯示上標 |
    | ④ 9.86 條號位移 | **屬實，且不只 9.86** | 修 3 支 + 加 BLOCK 檢查 |
    | ⑤ revisions 缺 9 項 | **9 項全部屬實** | 改為全文日期聯集 |
    | ⑥ 刪除條號政策不一致 | **屬實** | 補 9.7、9.89，政策明寫 |

    - **① 是「原文自己不一致」，不是我們抄錯**：健保署 115/7/23 版 9.69 術前輔助的非鱗狀條文寫「限使用 Pexeda、Apeta 或 Pemetrexed Sandoz」（3 項），但**同一版 9.26 Pemetrexed 第 6 款**描述同一個術前輔助療法時寫「Pexeda、Apeta、Pemetrexed Sandoz 或 Alimta Avos」（4 項）。審稿觀察到的不一致是真的，**但主體是健保署，不是本系統**。→ **不改條文（逐字忠實不可破）**，改在該子項掛 `xrefNote` 橘色警示，寫明兩處差異與條號，讓個管師送審前自己判斷。**教訓：發現「我們與原文不一致」之前，先確認「原文與原文自己是否一致」——跨章節描述同一療法的品項清單，是法規文件最常見的自我矛盾點。**
    - **② P151 的「六欄全不給付」是對的，但畫面會誤導**：官方 PD-L1 對照表的表頭只有 6 支藥（pembrolizumab／nivolumab／atezolizumab／avelumab／durvalumab／cemiplimab），**沒有 dostarlimab 欄位**；而子宮內膜癌唯一給付的 ICI 就是 dostarlimab。所以「6 欄全部尚未給付」在表格邏輯內完全正確，但使用者選婦癌看到這列會以為 EC 沒有 ICI 給付。審稿建議「把『不需檢附報告』放到獨立註記」——但我們的文字層**沒有**那一格（他可能讀到 P141 那列的串行）。→ **不編造不存在的格子**，改加 `rowNote` 說明「本表 6 支藥皆未給付；EC 的給付藥品為 dostarlimab，見第2款(10)，該藥不在本表欄位內」。**教訓：表格「查不到答案」與「答案是否定的」是兩件事，顯示層要能區分。**
    - **③ 指數 9 遺失**：原文 6 處皆為 `1×109/L`，我們有 2 處變成 `1×10 /L`（9.55 第3子項、9.114 第2子項）。V5.1.0 的上標修復規則只涵蓋 `mm3`／`mg/m2`／`1.73m2`／`10-3`，**沒有 `×10ⁿ` 這型**。順手全庫掃描又抓到 **6 處 `/ mm3`**（9.43／9.79／9.104）——我原本的 regex 是 `/mm\s*3`，但這些寫成 `/ mm3`（**斜線後有空白**），漏掉了。→ 兩型都修，`validate` 的單位檢查一併補上含空白變體與 `×10` 缺指數。**教訓：單位／指數的修復規則要以「掃出 0 筆」為驗收標準，不是「修好我知道的那幾個」。**
    - **④ 條號位移不只 9.86**：官方 9.86 是「無編號前導句 + 1./2./3.」，我們把前導句編成 item 1，導致官方 3 項變成我們 4 項——個管師引用「第 4 項」回覆審查委員會對不上。全庫掃描（判準：item 1 在原文中前面接的是標題括號 `)` 或 `：`、且原文另有官方 `1.`、且 items ≥2）抓到 **3 支：9.19、9.68、9.86**。→ 三支都把前導句移入 `lead`、其餘重編 1..n；並加 `validate` **BLOCK 12** 讓這型永遠不會再溜過。
    - **⑤ revisions 9 項全對**：我原本只從**藥品標題括號**抽日期，但新增條文常把生效日寫在子項尾端。改為「標題括號 ∪ 條文全文所有民國日期」去重、依年月日排序，共補回 **17 筆**（9.11 從 1 筆變 7 筆最誇張）。
    - **⑥ 刪除條號政策**：官方保留 `9.7.（刪除）`、`9.89.(刪除)`，我們沒收錄；但 `9.84.Copanlisib (如 Aliqopa）(刪除)` 有收錄——**差別只在 parser 抓不抓得到藥名**。依本系統「原文完整呈現」定位採**完整收錄法**：補上 9.7、9.89（灰階已刪除、附官方修訂日），「關於本系統」明寫收錄政策，並加 `validate` **BLOCK 11**（官方條號一律要收錄）。藥品數 151 → **153**。
    - 教訓總結：**審稿的「診斷」比「解方」可信**（呼應 Kit 坑 #76）。這次 6 項的診斷全部有價值，但其中 2 項的解方會讓我們寫入不存在於原文的內容——**照解方做就會製造新的錯誤**。核查要核到原文，不是核到審稿的敘述。

51. **【V5.7.0 機械式全文差異回歸：從 160/167 誤報收斂到 0/0，途中撿到 2 個真錯】**
    - 審稿建議「跑一次機械式全文差異回歸」。實作 `diff_source.py`，**雙向比對**（兩個方向問的是不同問題）：
      - 正向：原文每一句是否都在出版檔 → 抓**漏抄**
      - 反向：出版檔每一句是否都在原文 → 抓**錯字／幻覺／重複**
    - **第一次跑：漏抄 160、疑錯字 167 —— 幾乎全是誤報。** 收斂過程本身就是這條坑的重點，五類「可解釋差異」必須先扣掉，否則報告沒人看（呼應坑 #42）：
      | 誤報來源 | 為什麼 | 扣除方式 |
      |---|---|---|
      | 日期括號 | 生效日不影響條文內容，且常跨行截斷成 `(89/10/1、` | 移除成對／未閉合日期括號 + 散落民國日期 |
      | 條號 | `1.` `(1)` `Ⅰ.` 在兩邊的歸屬不同 | 切句後**反覆**剝句首日期括號與條號 |
      | 藥品標題行 | 存成 `name`/`brand`/`form_no`/`revisions` 不在條文裡；且**標題會跨行** | 讀到第一個「：」為止才算標題 |
      | 父/子合併 | 父項目 items 是子藥物的合併複本，逐節必然對不上 | 逐節對不上再用**全庫底本**兜底（兜到＝歸屬差異，非遺失，本版 98 筆） |
      | 我們的正規化與註記 | `mm³`/`m²`/`10⁻³`/`×10⁹` 是顯示層轉換；`rowNote`/`xrefNote` 是我們寫的 | 比對前還原成原文寫法；註記欄位不參與反向比對 |
    - **一個折磨很久的細節：`NFKC` 把上標負號 `⁻`(U+207B) 轉成 `−`(U+2212 MINUS SIGN)，不是 ASCII `-`。** 我的標點移除表只有 `\-–—`，少了 `−`，於是 `10⁻³` 正規化後變 `10−3`、原文 `10-3` 變 `103`，永遠對不上。**教訓：做 Unicode 正規化比對時，要實測 `NFKC` 的輸出字元，不要憑印象假設它會轉成 ASCII。**
    - **扣完誤報後剩下的 2 個真錯**：
      1. **9.5 父項目條文是 115/6/1 之前的舊版**（寫「除 PHYXOL、GENETAXYL CREMLESS 及 TAXOL 之外」），而子項目 9.5.1 已更新（「除 GENETAXYL CREM LESS 及 TAXOL 以外」，PHYXOL 已不在給付、且多了含 dostarlimab 的第 3 項）。**這是坑 #36「部署端更新只改 items、父項目會落後」的實證**——而且後果是：未選癌別看 9.5 讀到舊條文、選婦癌時 9.5 不會顯示 dostarlimab EC 那條。→ 改成**父項目的 items 一律由子項目重建**（沿用父原有的 `【9.x.y …】` 標記，只換條文），並重算 `cancers`／`pre_review`／`review_scope`。全庫 7 個父項目中**只有 9.5 不同步**，其餘 6 個不動（避免改動正確資料）。
      2. **9.132 有 5 處子項多了前導 `.`**（原文是 `(1)治療新診斷…` 無點）。判準要**在該藥自己的段落內**比對前綴，不能用全文 `find`——9.68 sub(3) 一開始被誤判要刪點，因為同一句話在 9.49 出現過且前面是 `II.`。**教訓：以「原文長怎樣」為判準時，一定要限定在正確的段落範圍內取樣。** 對照組：9.67／9.68 的 `(1).` 前導點**是原文就有的**，保留。
    - 最終 **漏抄 0／疑錯字 0／未收錄條號 0**，並把這支腳本納為 `validate` 的 **BLOCK 13**（回歸不通過就不得出版）。
    - 教訓：**「全文比對」的價值不在第一次跑出多少筆，而在你能不能把誤報壓到 0——壓到 0 之後，任何一筆新出現的差異都是真的。** 這才是可長期掛在建置流程上的閘門。

52. **【V5.7.0 三項審稿功能修正：狀態要放在唯一的過濾函式裡】**
    - ① **「只看常用」原本是渲染後用 CSS 隱藏**，`getFiltered()` 不知道 `favOnly`，所以統計數字寫「153 個藥物項目」而畫面只剩 3 張卡、癌別／類型 pill 的計數也不會變。→ 併進 `getFiltered()`，並讓癌別／類型 pill 計數與 stats 一起吃這個條件；stats 加「｜只看常用」標示。
      - **跨 script 區塊的狀態交換**：常用開關在 cdq 那個 `<script>`、過濾函式在主程式 `<script>`，兩邊看不到彼此的變數 → 用 `window.cdqFavOnly` / `window.cdqFavSet` 當狀態、`window.cdqRerender()` 當觸發出口（沿用坑 #46 的 `window.cdqClauseText` 模式）。**不要為了共用變數把 IIFE 拆開。**
      - 教訓：**「渲染後再用 CSS 隱藏」永遠會讓計數說謊。** 任何影響「有幾筆」的條件都必須進唯一的過濾函式。
    - ② **中文空白正規化只做一次相鄰配對**：`([\u4e00-\u9fff])\s+([\u4e00-\u9fff])` → `$1$2` 對「轉 移 性」只會消掉第一個空白（因為第二個中文字被吃進 match 當結尾，下次比對從它之後開始）。改用 **lookahead** 版 `([\u4e00-\u9fff])\s+(?=[\u4e00-\u9fff])` → `$1`：被消耗的只有前字＋空白，下一輪能從該字繼續。**刻意不用 lookbehind**（舊 iOS Safari 不支援）。
    - ③ **搜尋比對用正規化字串、反白卻用原始字串** → 「找得到卻沒反黃」。改成 `highlightRe()`：把查詢字串拆成字元、**字元之間允許 `\s*`**，這樣「轉移性乳癌」能反白到原文的「轉移性 乳癌」。
    - 教訓：**任何「比對」與「呈現比對結果」的兩段程式，必須共用同一套正規化規則**，否則使用者會看到互相矛盾的畫面。

53. **【V5.7.1 我自己違反了自己寫的規則兩個版本：cancerTags 繞過管線被手改】**
    - CLAUDE.md 的對映表明寫：**「不要手改 `cancerTags` —— 跑 `derive_v51.py` 全庫重推」**。但 V5.6.0／V5.7.0 為了修審稿問題，我**直接編輯出版檔的 `drugs` 陣列**（從 index.html 回抽 → 改 → 寫回），完全繞過了那支腳本。
    - 後果不是立刻壞掉，是**溯源靜默流失**：稽核檔的 `cancerTagSource` / `cancerTagRule` / `semanticHash` **571 個 item 全部變成無**。連帶 `validate` 依賴這些欄位的 WARN 檢查失去判別力——「含癌名卻判為 `*`」從 24 筆膨脹成 128 筆噪音（**報告一旦變噪音就等於沒有**，坑 #51 同一個教訓）。
    - **好消息是資料本身沒壞**：寫 `rederive.py` 用規則對「當前已修正的條文」重推，與出版檔逐節點比對 —— **568 個 item ＋ 749 個 subitem，不一致 0 筆**。手改沒有破壞語意規則，只是丟了帳。
    - 做法：
      1. `rederive.py` 重建溯源欄位（不靜默覆蓋——**不一致就報、不寫檔**）。
      2. 新增 `validate` **BLOCK 14：出版檔的 `cancerTags` 必須等於規則重推的結果**。這條之後，繞過管線手改標籤會直接讓建置失敗。
    - **順手修掉一個文件腐化**：對映表寫「9.69 病名對映 → `MAP_969`（改造腳本 `build_v5.py`）」——那是 **V5.0.0 時代**的東西，V5.1.0 起 9.69 已改由通用規則處理（其子項標題本身帶病名，`scan_diseases` 抓得到），`MAP_969` **早就不在管線裡了**。我照著這行文件去寫 `rederive.py` 的 9.69 特案，結果 `getattr(derive_v51, "MAP_969")` 直接 AttributeError——**過期的文件把我引導到一個不存在的東西上**。這是坑 #44「自我描述型文案會腐化」的第二次踩，而且這次腐化的文件**騙到了 Claude 自己**。
    - 教訓三層：
      1. **「不要手改 X」這種規則，光寫在文件裡不會被遵守**（我自己就是反例）——要有一個會讓建置失敗的檢查，規則才真的存在。呼應 Kit 坑 #7「資料合規驗證要擋出版」。
      2. **繞過管線的代價通常不是資料錯，是帳消失**——資料看起來一樣，但「為什麼是這個值」的答案不見了，下一個人（或下一個 Claude）只能重新猜。
      3. **文件裡指向具體檔名／變數名的句子，是最容易腐化也最容易誤導的一種**。它們該跟程式一起被檢查。

54. **【V5.7.2 兩套母圖收斂成一套幾何：小尺寸的問題不能靠「不做小尺寸」解決】**
    - Sela 的提問：「logo 是不是不要有太小的，愈小你愈容易出錯？」——方向對，但**診斷要修正**：
      - 那兩個錯（分界訂太保守、雷達意境消失）**都與尺寸無關**。前者是「掃描線剛好躺在水平十字線上，指標永遠讀到 0」，任何尺寸都會給我同樣的錯誤答案；後者是「哪個特徵承載身分」的設計判斷失誤（雷達的識別特徵是**十字刻度穿出圓環**，不是環的數量）。
      - 真正的弱點是：**我看不見，只能靠指標或靠人的眼睛；而我那次過度信任了一個自己沒驗證過的指標。**
    - **而且「不做小尺寸」不會消除小尺寸問題，只會把它交給更差的縮圖器**：分頁 favicon 本來就是 16px、iOS 桌面捷徑由系統縮放。不給 16px 檔，作業系統就拿 512px 直接縮——那正是我們一開始看到的那團糊。
    - 能減少的不是尺寸，是**我在小尺寸上做的獨立決策數量**。所以 Sela 拍板方案 A，把「兩套幾何」收斂成「一套幾何 + 一套母圖」：
      | 資產 | V5.5.1–V5.7.1 | **V5.7.2（方案 A）** |
      |---|---|---|
      | `app-logo.svg`（分頁 SVG favicon、36px 工具列）| 雷達簡化版 | 雷達簡化版（**唯一的簡化幾何**）|
      | `favicon-16x16.png` / `favicon-32x32.png` / `favicon.ico` | 各自用雷達簡化版光柵 | **一律由完整版母圖縮** |
      | ≥48px 全部 | 完整版 | 完整版（不變）|
    - **代價明寫**（量測過才敢說）：完整版在 16px 的環數 0 條、32px 剩 1 條 → `favicon.ico` 在極舊瀏覽器與書籤列會稍糊。**接受**，因為 ico 只在那些場合出現，而每天真正在看的分頁 favicon 走 SVG（向量渲染、任意尺寸都清楚）。用「同步兩套幾何的長期負擔」換「舊環境稍糊」是划算的——§10.4 rule 4 本來就警告兩套母圖會變成兩個 logo。
    - **流程改動（比資產改動更重要）**：**任何 ≤48px 的視覺資產，交付訊息一律附「原寸＋放大」對照圖，Sela 看過才算完成**，已寫進發版同步清單。
      - 理由是成本結構不對稱：**小尺寸的錯人一眼就看得出來（這次一輪就抓到），但我要靠指標、而指標會騙人。** 最有效率的分工不是我再多寫三個量化指標，是把那一眼放進流程裡。
    - 教訓：**當「我做不好某件事」時，先分清是「這件事本身難」還是「我的驗證方式不可靠」。** 這次是後者——移除能力（不做小尺寸）會讓結果更糟，補上驗證（對照圖給人看）才是對的方向。

55. **【V5.7.3 手機搜尋列被 header 吃掉：`position:sticky` 的 `top` 被寫死成常數】**
    - 症狀（Sela 實機截圖）：手機上 header 佔掉約 1/4 螢幕，**搜尋列被 header 蓋掉一半**，placeholder 只看得到下緣。
    - **根因**：`.search-section { position: sticky; top: 60px; }` —— 這個 `60px` 是**寫死「topbar 高 60px」的假設**。桌機成立，手機不成立：手機的 `@media (max-width:768px)` 讓 `.brand-sub` 變 `white-space:normal`、`.brand-data-date` 與 `.brand-last-check` 各自 `display:block`，於是副標題長成三行（「彰濱秀傳癌症中心」＋「程式參考的資料是 115年7月23日公告，115年8月1日生效」自己又折兩行＋「最後檢查 N 天前」），**實測 topbar+搜尋列共 140 CSS px**。搜尋列黏在 60px 處 → 正好卡在 topbar 中間。
    - 做法（兩層，第一層才是根治）：
      1. **把 topbar 與搜尋列包進同一個 `.header-stack{position:sticky;top:0}`**，內部自然堆疊 —— **不再需要知道 topbar 有多高**。零計算、零 JS、不會因為文案長度變化而失效。
      2. 壓縮手機 header 高度：資料日期做成長短兩版（`.dd-long` / `.dd-short`，手機顯示「115/7/23 公告 · 8/1 生效」）、隱藏「最後檢查」（資訊價值低於垂直空間，且「關於本系統」查得到）、`.brand-sub` 收回單行 + ellipsis、topbar/搜尋列 padding 縮小。**實測 140 → 92 CSS px（省 34%）**。
      - 注意：資料日期的 `data-date` / `data-effective` 屬性被「新版偵測」與「複製條文的出處行」讀取，所以**長短兩版包在同一個 `.brand-data-date` 元素內**，屬性不動。
    - 教訓：**`position:sticky` 的 `top` 只要是常數，就是一個「另一個元素多高」的隱含假設，而那個高度會隨字型、語言、文案長度、斷行改變。** 正解是讓瀏覽器自己算——把要一起黏的東西包成一個 sticky 容器。這跟坑 #34／#45（flex 沒 `min-width:0`）同一個家族：**把版面決定權交回排版引擎，不要自己算數字。**
    - **驗證踩到的兩個坑**（下次用 WeasyPrint 驗手機版時會再遇到）：
      1. 原檔的 `@media print` 有 `.topbar, .search-section { display:none !important }`（列印不該印 header，合理）——**WeasyPrint 以 print media 渲染，所以 header 永遠是空白頁**。要在測試頁覆寫回 `display:block`。
      2. **WeasyPrint 不會依頁寬觸發 `max-width` 媒體查詢**，設 `@page{size:390px}` 也不會套用手機規則 → 測試頁要**手動內聯** `@media (max-width:768px)` 的內容，才量得到手機版高度。

56. **【V5.8.0 事前審查是「條文層」的要求，不是「藥品層」的屬性 —— 一個已造成臨床延誤的錯】**
    - 來源：胸腔科張醫師實機回報。9.46 TS-1 選「肺癌」後，畫面仍掛橘色「事前審查」標籤與「送健保署事前審查核准後方可使用」。**但健保署原文全段只有一句事審，位置在「2.胃癌」底下的 (2)** —— 肺癌、胰臟癌、膽道癌都不需要。
    - **這不是顯示瑕疵，是已發生的臨床傷害**：醫師回報「之前我門診肺癌患者要開 TS-1，當時就被醫院資訊系統擋住而無法開立」。而個管師拿我們的工具去查，**得到的是錯的確認**，等於幫錯誤背書。
    - **全庫盤點**（`pa_matrix.py`，判準＝模擬渲染後使用者在該癌別實際看得到的條文是否含事審字樣）：153 支中
      - **★分歧 5 支**：9.34 Sorafenib（泌尿癌/腎細胞癌不需）、9.37 Bevacizumab（肝膽胰胃癌/HCC 併 atezolizumab 不需）、9.41 Pazopanib（泌尿癌不需）、9.46 TS-1（肺癌不需）、9.48 Eribulin（乳癌不需）
      - 全癌別皆需 101、皆不需 46、僅附表推定 1（9.32）
      - **反向漏標 0** —— 問題是單向的過度標示，共 5 個（藥×癌別）組合
      - 這五支都是「老適應症不用事審、後加的新適應症才要」，**分歧是法規歷史堆疊的自然結果**，不是資料錯誤。
    - **關鍵發現：不需要新增資料欄位。** 事審條文都掛在特定適應症 item 底下的子項，而**那個 item 帶癌別標籤** —— 所以「這個癌別要不要事審」的資訊已完整存在於現有結構。改成**載入時由條文推導**（`paClausesFor(d, sel)` 直接沿用渲染器的篩選邏輯）：
      - 不新增欄位 → 沒有資料遷移、沒有「條文改了但旗標忘了改」的同步風險
      - 判準與畫面同源 → **不可能出現「條文沒顯示、標籤卻在」的矛盾**（這正是原本的病）
    - 呈現（Sela 拍板方案 C + 新增「部分事審」標籤）：
      | 情境 | 標籤 | 底部說明 |
      |---|---|---|
      | 全癌別皆需 | ⚠ 事前審查 | 送審方可使用 + 依據條號 |
      | **不同癌別規範不同、未選癌別** | **⚠ 部分事審**（虛線框）| 「本藥不同癌別的事審規範不同，請看條文上標示「需事審」者」+ 依據條號 |
      | 選了癌別、該癌別需要 | ⚠ 事前審查 | 送審方可使用 + 依據條號 |
      | **選了癌別、該癌別不需要** | **不掛標籤、左邊框不上橘** | **明確否定**：「已依「肺癌」篩選，該適應症之給付條文**未載事前審查要求**」|
      | 條文未載、僅有附表（9.32/9.41 泌尿癌）| ⚠ 附表待確認 | 「條文未載事前審查，但有附表 X；送審與否請依附表規定確認」|
      - 條文層一律掛「需事審」小標籤（跟「通用」同風格、橘色）——**唯一真相所在，讓人一眼看到是哪一條**。
      - **明確的否定比沉默好**：這次的傷害來自「以為要送審」，張醫師的反應（「肺癌TS-1不需事前審查！」）本身就是在尋求一個明確否定。但用詞只描述我們看到什麼（「條文未載」），**不替健保署下「不需要」的結論**。
    - **同步修掉的放大風險**：「複製條文」原本無條件在尾端加「※ 本藥需經健保署事前審查核准後使用」。那段文字會被貼到病歷／送審備註，**錯誤跟著離開系統、而且離開後沒有上下文可以自我修正**。改為依篩選後的條文重算，四種情境各自輸出對應文字。
    - **「只顯示需事前審查」篩選與癌別 pill 計數**也改用該癌別判斷（否則「肺癌＋只看事審」會跑出 9.46）。
    - **實作時踩到的自己的病**：`paState()` 未選癌別時我原本回 `need = (scope==='all' || scope==='partial')` —— 那對 partial 又變成過度斷言，等於把剛修的病搬到另一個分支。改成未選癌別時 `need` 只在 `all` 為真，partial 走專屬訊息。**教訓：修「過度斷言」時，要檢查所有分支是不是都改了，別留一個。**
    - 教訓（最上位）：**「這個屬性屬於哪一層」是資料模型最容易錯、且錯了最貴的判斷。** `pre_review` 當布林值放在藥品層看起來很自然，但法規的真實層級是「條文」；層級一錯，任何顯示邏輯都救不回來。判準是問：**「同一個實體的不同部分，這個屬性會不一樣嗎？」** 會 → 它就不屬於實體層。這跟坑 #83（衍生資料的消費位置）是同一類架構層錯誤。

## 四、發版同步清單 (Rule 1 補完)

每次發版必須同步改的位置 (這次 V3.10/V3.11 中間有 5 版斷檔的教訓):

0. **SELA Kit 對齊版本**:目前對齊 **Kit V1.27.0**(見坑 #44);Kit 跳 ≥3 個 b 版時走「落差區間評估」
0.5 **跑 `python3 diff_source.py <出版檔>`,漏抄/疑錯字/未收錄條號必須全 0**
0.6 **跑 `python3 rederive.py <出版檔>`,標籤不一致必須為 0**(改過條文就要重跑,順便重建溯源欄位)
0.7 **動到任何 ≤48px 視覺資產 → 交付訊息必附「原寸＋放大」對照圖給 Sela 看過**(坑 #54;我看不見小尺寸,只能靠指標,而指標會騙人)
0.8 **跑 `python3 pa_matrix.py <出版檔>`,確認事審×癌別分類與上版一致**(條文更新後分歧藥可能增減,坑 #56)
1. `index.html` 行 ~1521 `<span class="brand-version">vX.Y</span>`
2. `index.html` 行 ~1526 `<span class="brand-data-date" data-date="115.X.Y" data-effective="115.X.Y">` 顯示文字
3. **跑 `validate_v51.py`,BLOCK 必須為 0** (資料有動時)
4. `CLAUDE.md`「踩過的坑」加新編號 (只加不刪)
4. `CLAUDE.md`「版本歷程」表加 row
5. `CLAUDE.md`「一句話總結」更新

## 四、版本歷程

| 版本 | 重點 |
|------|------|
| V3.0 | 漸層背景、玻璃毛霧 sticky header |
| V3.1 | 結構化條文呈現、長條文不截斷 |
| V3.2 | 逐癌切片(9.69 手工 + 28 自動) |
| V3.3 | 父項目從子合併 + 精細逐癌切片 (33 個跨癌藥 100% 切片) |
| V3.4 | GitHub Actions 自動檢測健保署資料更新 |
| V3.4.1 | 加多重備援策略 (r.jina.ai 等) |
| V3.4.2 | 偵測到更新時自動下載 PDF**,上傳為 artifact,Issue 直接附下載連結 |
| V3.10 | Header 加「最後檢查 N 天前」(GitHub Actions API);資料日期文字改為「程式參考的資料是 115年4月23日」+ data-date 屬性 |
| V3.11 | 跟進 115/5/22 公告 (6/1 生效):補回 9.49 Abiraterone / 9.99 Gilteritinib (V3.9 parser 漏抓的歷史藥);新增 9.135 Capivasertib / 9.136 Fruquintinib(V3.11 起誤拼為 Fruquitinib、V5.5.3 修正、見坑 #49);9.84 Copanlisib 標"已刪除"(灰底淡出 + 紅色標籤);Header 改「115年5月22日公告，115年6月1日生效」|
| V3.12 | 「關於」面板的資料來源加上「前往健保署藥品給付規定下載最新版」連結 (https://www.nhi.gov.tw/ch/cp-7593-ad2a9-3397-1.html),方便 Sela 拿到下次更新 PDF |
| V4.0.0 | **兩層 diff 重做 115/6/1 更新**:13 個法規變動(9.69 術前輔助 nivolumab + P016 表格列、續用評估改 i-RECIST、9.3/9.4/9.5.1 給付架構重整、9.2/9.20/9.51/9.66/9.129/9.130 條文修訂、9.84 刪除日期更正為 113/6/1)。**寬驗證修 3 類既有 bug**:全庫 search_text 重生(含 per_cancer)、補回 9.68/9.19/9.56 前言。多抓到 9.51、排除假陽性 9.71 |
| V4.1.0 | **對齊 SELA Kit**（.gitignore / favicon 套件 / 根 README / SELA-handoff）。**新增前端功能**：每藥「複製連結」深連結、「常用清單」(localStorage 星號 + 只看常用)、「最近搜尋」chips、「副作用照護」跳轉癌症希望基金會、列印優化(@media print 只印展開的藥)。候選工作 #1 自動切片驗證(32 藥 0 可疑)、#2 自動下載機制審查一併交付。 |
| V4.1.1 | **修 bug**：「只顯示需事前審查」開關原本是篩選面板**最後一個** section，手機上落在摺疊線以下、要往下捲才看得到（使用者誤以為「勾了別的才出現」）。移到面板**最上面**（癌別之前），開啟即見。 |
| V4.1.2 | **修真正的根因**：篩選面板（與「關於」面板共用 CSS）根本捲不動——`.filter-body` 是 `flex:1` 但沒 `overflow-y`/`min-height:0`，flex 子項剛好填滿面板、內容被裁掉且不出捲軸（電腦滑鼠也滾不動）。修法：`.filter-body` 加 `overflow-y:auto; min-height:0`，面板改 `overflow:hidden`。V4.1.1 把 事審 移上來只是讓它看得到，沒解決捲動。 |
| V4.1.3 | **UI 整理**：篩選 pill 從 `flex-wrap`（變寬度→參差不齊）改成**固定 2 欄 grid**（等寬對齊），名稱靠左、數量靠右（`margin-left:auto`），圓角由 100px 收成 10px 的整齊 chip。解決面板「凌亂」感。 |
| V4.1.4 | **修 pill 數字出框**：5 字長癌名（大腸直腸癌/血液淋巴癌/肝膽胰胃癌）把數字擠出框外。名稱改包 `.pill-name`(`flex:1;min-width:0;overflow:hidden;ellipsis`)、數字 `flex-shrink:0`，數字永遠留在框內。 |
| V4.1.5 | **pill 改上下兩層**：名稱在上（整顆寬度、不再截斷長癌名）、數字置中在下。dot/名稱包 `.pill-main`，數字獨立一行；隱藏 check（active 已用填色表示）。 |
| V4.2.0 | **健保 115/6/23 更新（115/7/1 生效）**：兩層 diff 找出本次唯一實質變動是 **9.18 Trastuzumab 早期乳癌**——新增生物相似藥 Eirgasun vial 420 mg 療程上限、新事審代碼 C50P3/P4/P5、pCR/non-pCR 與 ER 狀態的 9/18/14 療程分流、115/6/30 過渡條款。資料日期→115.6.23、生效日→115.7.1。新版 PDF 是乾淨文字檔(非掃描)，擷取精確。 |
| V4.2.1 | **修 9.98 跨段落 OCR 滲入**（子程式 OncoPA 回報）：9.98 Pemigatinib 的 `per_cancer["血液淋巴癌"]` 切片吃進整段 9.99 Gilteritinib，並害 9.98 被誤分到血液淋巴癌。items 早已乾淨，但 V4.0.0 起 search_text 納入 per_cancer 讓它復現。修法：(1)移除幻影切片+癌別標籤+重生 search_text；(2)加**載入時 de-bleed 守門員**(截斷 per_cancer/items 內的外來 9.X. 段落、清空切片則移除並同步 cancers、重生 search_text)，部署端每次載入自我清理。 |
| V4.3.0 | **健保 115/7/23 更新（115/8/1 生效，大更新）**：2 新藥 9.137 Cabazitaxel(前列腺)、9.138 Aumolertinib(肺)；13 修訂(9.2/9.3/9.5/9.5.1/9.18/9.24/9.26/9.29/9.45/9.69/9.80/9.83/9.99)。主軸：aumolertinib 進多個 EGFR-TKI 擇一清單、子宮內膜癌免疫組合擴充、9.18 加 Herzuma/Ogivri 生物相似藥、9.99 分移植前/後。**items(預設檢視)全部依乾淨新文字重建正確**；**per_cancer 切片：whole-item 藥(9.2/9.18)內容已更新，但 subitem/manual 切片藥(9.3/9.5.1/9.69)+新增 item 未進切片，需 dev 端重跑 slicer**。 |
| V4.3.1 | **補新藥事審旗標**：9.137 Cabazitaxel、9.138 Aumolertinib 兩個新藥都須事前審查，但 V4.3.0 加入時漏設 pre_review，導致不進「只顯示需事前審查」篩選/無事審標記。補 pre_review=True。 |
| V4.4.0 | **回應外部程式審稿意見**：(P0)清 14 個 \u0002 控制字元(Herzuma 去字元、PD-L1/MSI-H/EGFR-TK/Non-epithelioid 補連字號)；癌別篩選加**警示橫幅 + 「完整給付原文」<details> 摺疊**(永不隱藏原文，暫時緩解 per_cancer 切片漏條的 P0 風險)。(P1)搜尋正規化 normSearch(NFKC+去控制字元+併 CJK 內部空白+連字號正規化)；列印永不空白(beforeprint 若無展開項→印全部)；更新偵測改「較新才報」(民國日期數值比較，非 !=)。 |
| V5.0.0 | **資料模型改造：`per_cancer` 切片 → `cancerTags` 條文標籤**。前端癌別篩選改為直接篩原文（`items[].cancerTags`，通用條文標 `"*"`），移除 `per_cancer`／`common_data`／`is_manual_split` 三欄與兩支切片渲染函式、以及 V4.4.0 的警示橫幅與「完整原文」摺疊膠帶。失敗模式由「漏條被藏」翻轉為「頂多多顯示」。自動補回舊切片漏掉的 9.2 第8/9款、9.5.1 婦癌、9.69 婦癌；修正 9.48 Eribulin 混入 9.49 Abiraterone 前列腺癌條文的污染；9.69 以病名對映表做到子項級標籤（21 子項全中）。檔案 1.24 MB → 1.06 MB。詳見坑 #37、#38 |
| V5.1.0 | **cancerTags 全庫從原文重推**（外部程式審稿後）。推導優先序：行政共通條文 → `["*"]`；病名同義詞辨識 → 癌別；標不到 → `["*"]`（與行政條文分流記帳）。核心語意規則：標題指明癌別者子項為其條件、傘狀條文才由子項各自標籤。修正 9.2/9.10/9.16/9.12/9.5/9.5.1/9.9/9.95 等誤標與缺標；藥物層 `cancers` 改為標籤聯集自動推導（補回 9.12.1 肝膽胰胃癌、9.13＋9.14 其他癌、9.95 乳癌＋頭頸癌、9.69 婦癌）。從原文回填 44 筆截斷修訂日期、8 處未閉合括號、14 處上標（`m²`/`mm³`/`10⁻³`）。`pre_review` 父項改為自身 OR 子項並加 `review_scope`／`review_source`。新增建置端驗證 `validate_v51.py`（BLOCK 0 才可出版）。密碼更新並加入遮蔽／顯示切換。詳見坑 #39、#40 |
| V5.2.0 | **9.69 PD-L1／事審代碼對照表結構化**。原本壓平成 3,901 字一段的 29×6 矩陣解析成 `table{columns,rows[scope,code,cells,cancerTags]}`，以事審代碼為錨點、封閉儲存格用語逐格比對，**round-trip 逐字一致才寫出**。畫面只列出真的有給付的藥品＋PD-L1 門檻＋對應 IVD 平台，未給付收成一行；事審代碼做 badge；原表收進可展開橫向捲動表格。每列自帶癌別標籤 → 選肺癌 8 列/29、其他癌 3 列、乳癌與婦癌各 1 列。另：條文內 `Ⅰ. Ⅱ. i.` 層級標記改為換行縮排（只認羅馬數字）。驗證加入表格列數/格數/round-trip 的 BLOCK 檢查。詳見坑 #41 |
| V5.3.0 | **全文表格稽查 + 補回三類轉檔遺失**。用「重複片語次數 × 短行比例」掃過 153 個段落，確認全文僅 9.69 一張被壓平的表格（其重複片語 82 次／短行 66%，第二名 9 次／21%）。逐字元對比原文後補回：藥品層前言 `lead`（9 藥，含 9.20 Rituximab「用於抗癌瘤部分」——該藥另有非癌適應症，少這句會誤讀範圍）、藥品層歷次修訂生效日 `revisions`（716 筆，原本完全沒存）。確認控制字元 0 筆、其他實質條文遺失 0 筆。驗證加入「`lead` 必須逐字存在於原文」「`revisions` 須符合民國日期格式」的 BLOCK 檢查。詳見坑 #42 |
| V5.3.1 | 藥品卡改為**手風琴**（展開新的自動收起原本展開的），展開後 `scrollIntoView` 把標題捲回視野。同時修掉教學按鈕的事件順序 bug（原本靠冒泡階段的 `stopPropagation()` 擋不住 header 自己的 handler，改在 handler 內排除 `.edu-btn` 等按鈕）——手風琴會讓這個 bug 變嚴重，故必須一起修。詳見坑 #43 |
| V5.4.0 | **對齊 SELA Kit V1.27.0**（上次對齊 V1.9.0，落差 18 個 b 版，走「落差區間評估」不重跑首次對齊）。修掉三件舊債：①「關於本系統」面板停在 V4.x 的「逐癌切片」世界觀 + 資料日期停在 115/5/22 → 改寫為 cancerTags 說明 + 115/7/23；②`.gitignore` 專案特定段列的是 V5.0.0 已確認不存在的 dev pipeline 檔 → 換成真實建置端腳本；③2 處 catch 區塊 `console.log` → `console.warn`。依 Kit §8.3 產出 `SELA-logo-prompt.md`（範本 B 醫療專業型、底色 #436f8a、不指定主體）；依 §9 先建 SELA 歸屬印記於「關於」面板底部並引用獨立 `favicon/sela.svg`。優化體檢：4 條 OPT 中 3 條技術不適用、OPT-4 已實質落實 → 0 項可升級。詳見坑 #44 |
| V5.4.1 | **修手機版面 + 星號顯示**。①`.subitem-text` / `.clause-text` 補 `min-width:0`——V5.2.0 起子項內含 `min-width:720px` 的 PD-L1 表，flex 子項預設 `min-width:auto` 導致手機整頁被撐開、內層橫向捲動失效（坑 #34 第二次踩）；②半形 `*` 在中文段落換行後像錯字 → 註記符號改上標 `※`（`footnoteMark()`）、`2*106` 顯示為 `2×10⁶`（`formatHierarchy()`），**皆為純顯示轉換、資料層與原文逐字一致**；③移除逐藥「副作用照護」按鈕（151 支藥同一個網址、佔標題列版面），改為「關於→相關資源」單一入口；④手機版標題列按鈕可換行、PD-L1 檢測平台獨立成行。詳見坑 #45 |
| V5.4.2 | **「複製連結」改成「複製條文」**（Sela：不是每個人都有這程式權限，深連結對接收端無用）。新增 `buildClauseText()` 產出純文字：跟著目前癌別篩選、含藥號/學名/商品名/`lead`/〔通用〕標記/「另有其他癌別條文 N 項未列入」、PD-L1 對照表逐列文字化、事前審查提示，並**自動附上資料公告與生效日期＋出處**。顯示層轉換（`*`→※、`2*106`→`2×10^6`、去中文字間空白）與畫面共用同一套規則。剪貼簿加 textarea + `execCommand` 後援（舊 iOS Safari / 非 https）。跨 script 區塊以 `window.cdqClauseText` 當唯一出口。修一個順序 bug：空白收斂原本在插入縮排之後、把縮排吃掉了。詳見坑 #46 |
| V5.5.0 | **App logo 上線（Kit logo §10.2 工作流 B）**。Gemini 生圖 → Claude 轉檔：連通區域分離外白底/主體白色（四角 floodfill 殘留抗鋸齒會害 bbox 抓錯）、重新裁切成主體佔 82%（白色佔比 6%→16.5%）、底色以亮度混合係數校正成精確 `#436f8a`。**依 §10.4 做兩套母圖**：完整版線稿靶心在 ≤48px 量測到 0 次明暗交替（整團糊）→ 另做簡化實心剪影版（實心白膠囊＋負空間圓環＋中心點，16px 4 次交替），**≤48px 用簡化版、≥64px 用完整版**，兩套同一組幾何常數。產出 svg/16/32/48/64/128/180/192/256/512/1024 + favicon.ico(16/32/48 簡化版)。`site.webmanifest` 客製 name/short_name/theme_color；`theme-color` 由 `#2c4f66` 改為 app 主題色 `#436f8a` 並與 manifest 一致；SVG favicon 改指 app logo（§4.1 V1.19.0）。**雙軌**：SELA logo 只留「關於」底部歸屬印記。兩處 base64 JPEG（37KB）改為檔案引用，index.html 反而縮小。詳見坑 #47 |
| V5.5.1 | **修 app logo 簡化版遺失雷達意境**（Sela 回報）。①簡化版改為「實心白膠囊＋負空間三層環（底色外環/白環/底色中心點）＋白色十字線畫最上層」——雷達的識別特徵是**十字刻度穿出圓環**，不是環的數量；②**修一個量測指標的錯**：V5.5.0 用「右半水平中線交替次數」判斷可辨識度，但水平十字線正好躺在中線上、整條全白 → 永遠量到 0，誤判完整版 48px 也糊。改用避開十字線的 35° 對角掃描後：完整版環在 16–24px=0 條、28–32px=1 條、**48px 起 7 條清楚** → 分界由「≤48 簡化」修正為 **≤32px 簡化 / ≥48px 完整**；`favicon.ico` 內嵌 16/32 簡化版、新增 `app-logo-48.png`（完整版）與 48px icon link。詳見坑 #47 補述 |
| V5.5.2 | **雙 logo 制落地**（Sela：我的 logo 找個地方放上）。依 Kit logo §9 把 SELA 歸屬印記放三處：入口密碼頁卡片最底（分隔線下、`by SELA` + 18px）、主畫面頁尾（左專案名／右 `by SELA` + 16px）、關於面板底部（V5.4.0 已有）；三處皆引用獨立 `favicon/sela.svg`。app logo 維持主視覺，尺寸比 64:18＝3.6:1、不並排。列印時頁尾隱藏。**修一個渲染爆版**：`sela.svg` 只有 `viewBox` 無固有寬高，只靠 HTML `width`/`height` 屬性時被 WeasyPrint 渲染成約 200px（撐爆入口卡片）→ 改在 CSS 明確鎖 `width`/`height`。詳見坑 #48 |
| V5.5.3 | **修 9.136 藥名拼字**：`Fruquitinib` → **`Fruquintinib`**（少一個 n，自 V3.11 錯到現在；舊拼法會讓使用者搜「fruquintinib」查不到這支藥）。同步重生該藥 `search_text`。**`validate` 新增 BLOCK 10：每支藥的 `name`/`brand` 必須逐字出現在健保署原文**——立即掃全庫 151 支，僅此一支不合格。教訓：從權威來源抄出來的欄位都要能溯源，包括被誤當成 metadata 的藥名；且同一份資料裡出現同一實體的兩種拼法（9.66 條文的 `fruquintinib` 是對的）本身就是可自動偵測的訊號。詳見坑 #49 |
| V5.6.0 | **外部審稿 6 項核查後修正**。屬實照改：①9.55／9.114 `1×10 /L` → `1×109/L`（＋全庫又掃到 6 處 `/ mm3`）；②9.86 條號位移 → 全庫掃描發現 **9.19／9.68／9.86 三支**把無編號前導句佔用了 `1.`，前導句移入 `lead`、條號重編；③`revisions` 改為「標題括號 ∪ 條文全文日期」去重排序，補回 **17 筆**（審稿列的 9 項全部補齊）；④補回官方保留的 `9.7`／`9.89`（刪除）並明寫完整收錄政策，**藥品數 151 → 153**。歸因錯誤但點出真問題：⑤9.69 少 `Alimta Avos` 係**健保署原文與 9.26 自我矛盾**（不改條文，加 `xrefNote` 橘色交叉差異註記）；⑥P151 六欄全「尚未給付」與原文一致（官方表頭無 dostarlimab 欄位），加 `rowNote` 說明 EC 的給付藥品與「查不到 ≠ 否定」。新增 validate **BLOCK 10/11/12**（藥名溯源／官方條號完整收錄／條號不得位移）＋單位檢查補含空白變體。詳見坑 #50 |
| V5.7.0 | **機械式全文差異回歸上線（審稿建議）＋三項功能修正**。新增 `diff_source.py` 雙向比對（原文↔出版檔），第一次跑 漏抄 160／疑錯字 167 幾乎全是誤報，扣除五類可解釋差異（日期括號、條號、跨行標題行、父子合併、我們的顯示層正規化與註記）後收斂到 **0／0／0**，並納為 validate **BLOCK 13**。途中撿到 2 個真錯：①**9.5 父項目條文是 115/6/1 之前的舊版**（子 9.5.1 已更新，害未選癌別讀到舊文、選婦癌看不到 dostarlimab EC 條文）→ 改為父項目一律由子項目重建（7 個父項目僅 9.5 不同步）；②9.132 有 5 處子項多了前導 `.`（判準必須限定在該藥段落內取樣，否則 9.68 會被誤刪）。功能修正：「只看常用」併入 `getFiltered()`（統計數字與 pill 計數不再說謊）、中文空白正規化改 lookahead 版、反白改為字元間允許空白以與 `normSearch` 一致。詳見坑 #51、#52 |
| V5.7.1 | **建置管線完整性修復（無資料變更）**。V5.6.0／V5.7.0 為修審稿問題直接編輯出版檔、繞過 `derive_v51.py`，導致稽核溯源欄位（`cancerTagSource`/`Rule`/`semanticHash`）**571 個 item 全部流失**，`validate` 的 WARN 失去判別力（「含癌名卻判為 *」從 24 筆膨脹成 128 筆噪音）。新增 `rederive.py` 用規則對當前條文重推並逐節點比對：**568 item ＋ 749 subitem，不一致 0**（手改沒破壞語意，只丟了帳），已重建完整溯源。新增 validate **BLOCK 14：出版檔標籤必須等於規則重推結果**——之後繞過管線手改會直接擋出版。順手修掉對映表指向 `MAP_969`／`build_v5.py` 的過期敘述（V5.1.0 起已無此特案，**這行過期文件把我自己引導到不存在的變數上**）。WARN 報告把 224 筆 `fallback-unclassified` 收成每藥計數（避免報告變噪音）。詳見坑 #53 |
| V5.7.2 | **兩套母圖收斂成一套幾何（方案 A，Sela 拍板）**。`app-logo.svg` 維持雷達簡化版並成為**唯一的簡化幾何**（供分頁 SVG favicon 與 36px 工具列）；`favicon-16x16.png` / `favicon-32x32.png` / `favicon.ico` **改由完整版母圖縮**，不再各自維護一套簡化光柵。代價已量測並接受：完整版 16px 環數 0、32px 剩 1，即 `favicon.ico` 在極舊瀏覽器與書籤列稍糊，但現代分頁走 SVG（向量、任意尺寸清楚），換掉「同步兩套幾何」的長期負擔。**流程改動**：任何 ≤48px 視覺資產，交付訊息一律附「原寸＋放大」對照圖給 Sela 確認（已進發版同步清單）——小尺寸的錯人一眼看得出來，我只能靠指標而指標會騙人。詳見坑 #54 |
| V5.7.3 | **修手機 header 吃掉搜尋列**（Sela 實機截圖）。根因：`.search-section{position:sticky;top:60px}` 把「topbar 高 60px」寫死成常數，但手機上副標題折成三行、實測 header 共 140 CSS px → 搜尋列黏在 60px 處正好卡在 topbar 中間。改為**把 topbar 與搜尋列包進同一個 `.header-stack{position:sticky;top:0}`**，不再需要知道 topbar 多高。同時壓縮手機 header：資料日期做長短兩版（手機顯示「115/7/23 公告 · 8/1 生效」）、隱藏「最後檢查」、副標題收回單行、padding 縮小 —— **實測 140 → 92 CSS px（省 34%）**。詳見坑 #55 |
| V5.8.0 | **事前審查改為條文層推導（胸腔科回報的臨床錯誤）**。9.46 TS-1 選肺癌時仍顯示「事前審查」，但原文只有「2.胃癌」底下那句要送審——醫師回報曾因此被醫院系統擋住、肺癌病人開不出 TS-1。全庫盤點（新增 `pa_matrix.py`）：153 支中**分歧 5 支**（9.34/9.37/9.41/9.46/9.48）、全癌別皆需 101、皆不需 46、僅附表推定 1，**反向漏標 0**。**不新增資料欄位**——事審條文掛在帶癌別標籤的 item 底下，資訊已在結構裡，改為載入時由條文推導（判準與畫面同源，不可能再出現「條文沒顯示、標籤卻在」）。標籤三態：`事前審查` / **`部分事審`（新，虛線框）** / `附表待確認`；選了癌別則依該癌別精確顯示，不需要時**明確寫「該適應症之給付條文未載事前審查要求」**（只描述所見、不替健保署下結論）。條文層掛「需事審」小標籤。「複製條文」尾註依篩選重算（原本無條件斷言，錯誤會跟著貼進病歷）。「只顯示需事前審查」與癌別 pill 計數改用該癌別判斷。詳見坑 #56 |

> **版號規則變更（V4.0.0 起）：** 本專案自 V4.0.0 改採 SELA Kit 嚴格三位版號 `V<a>.<b>.<c>`（每位 0–9、逢十進位）。V3.0–V3.12 為 Kit 前舊編碼，依「保留歷史 + 從下版開始嚴格」保留不動；本次為功能版，因 V3.x 早已超過 .9，依逢十進位由 b 進位至 a，落點 V4.0.0（屬進位、非破壞性大改版）。

> 註:V3.5–V3.9 的版本歷程未記錄於本表,改動內容散見「踩過的坑 #19–#27」。

```
專案結構:
├── index.html                              # 單檔主應用
├── CLAUDE.md                               # 本文件
└── .github/
    ├── README.md
    ├── workflows/check-nhi-update.yml      # 每週狙擊
    └── scripts/check_nhi.py                # 抓網頁 + 下載 PDF

開發端 (產生 index.html 用,不放 repo):
├── drugs_final.json                        # 145 個藥物原始解析
├── per_cancer_data.py / .json              # 9.69 手工資料
├── make_html_v6.py                         # 產生 drug_data
└── logo_b64.txt                            # SELA logo
```

---

## 六、佈署流程

### 6.1 一般版本更新
1. 改完 index.html 後本機測試
2. Git Pusher 打包 `Cancer Drug V3.X.zip`
3. 用 Sela 的自動部署工具推送

### 6.2 收到「健保署有新版」issue 時 (核心使用流程!)
1. **點 Issue 內 "前往下載" 連結** → Actions run 頁面 → 底下 Artifacts → 下載 `chap9-pdf-XXX.zip` → 解壓得 PDF
2. 開新 Claude 對話「健保署有新版了,請重新整理資料」+ 上傳 PDF
3. Claude 重做後得到新 `Cancer Drug V3.X.zip`
4. Git Pusher 推送
5. **改 `.github/scripts/check_nhi.py` 中 `CURRENT_DATA_DATE`** 為新日期 ⚠️
6. 關閉 GitHub Issue

如果 PDF 自動下載失敗,Issue 內會有原始健保署連結讓你手動下載。

---

## 七、Claude 重做資料時的處理重點

當 Sela 上傳新版 PDF 並說「健保署有新版了」,Claude 接手要做：

1. **重跑 PDF 解析** → 145+ 個藥物項目 (新版可能有新增藥物 9.135、9.136...)
2. **不要手改 `cancerTags`** — 跑 `derive_v51.py` 全庫重推。要調整就改 `DISEASES` / `UNIVERSAL_A` / `UNIVERSAL_B` 規則,再重跑
3. **跑 `validate_v51.py`** — BLOCK 為 0 才可打包;WARN 報告要掃一遍(尤其「含癌名卻判為 *」與「只靠單一模糊器官詞」)
4. **看差異報告** `report_02_tags.txt` — 每藥每條的新舊標籤、推導規則、`*` 原因都在裡面,確認 151 支全被校正過,不是只驗證幾個抽樣
5. **更新 CLAUDE.md**:版本歷程加新行、踩過的坑加新編號(如有新坑)
6. **打包成新版 Zip**:Cancer Drug V3.5.zip

---

## 八、下版候選工作

V4.1.0 已完成：#3 複製連結、#4 列印優化、#5 常用清單、#6 最近搜尋、#8 副作用照護(ecancer 跳轉)。
#1 自動切片驗證、#2 自動下載機制審查 已產出報告（32 藥 0 可疑；自動下載閉環仍待實機驗證，步驟見報告）。

**仍未做：**

1. **實機回歸測試（唯一還沒做的驗證缺口）** — 以下都只做過靜態渲染／node 模擬，沒有真瀏覽器：
   - 手機：PD-L1 對照表橫向捲動、標題列按鈕換行、手風琴捲動位置（V5.4.1）
   - 手機剪貼簿：「複製條文」在 iOS Safari 走 textarea 後援那條路（V5.4.2）
   - App logo：分頁 favicon、iOS「加入主畫面」、Android 捷徑（≤32px 雷達簡化版／≥48px 完整版，應像同一個 logo）
   - 「只看常用」重新渲染後的統計數字與 pill 計數（V5.7.0 剛改）
2. **9.103 `2*106 CD34+細胞`** — 健保署來源檔本身就寫 `2*106`（應為 2×10⁶），上游即已遺失指數。顯示層已轉成 `2×10⁶`，**資料層不自行改寫**，待原文更正。
3. **品質指標附件整合** — 國健署「癌症診療核心測量指標」強制申報 **60 項**（13 癌別）。屬「品質申報指標」領域，跟藥物速查不同；**待 Sela 決定放這個 SPA／獨立工具／併入 CCM Manual** 再做。
4. **ecancer 深連結** — 目前「關於→相關資源」只連首頁。要直連藥品頁需先取得 ecancer 全藥索引建學名→ID 對應表（僅覆蓋 5 癌、整體約 64% 命中）。
5. **自動下載 PDF 機制實機驗證** — 邏輯已審查無誤，待在 GitHub Actions 手動觸發驗證閉環。
6. **子項級標籤全面化** — 目前「一條混多癌」若有子項歸不出癌別就整條顯示（安全但不夠精準）。

---

## 九、一句話總結

V3.4.2 完成了「**健保署有更新 → 自動下載 PDF → 寄 Email 通知 Sela → 點連結下載 PDF → 給 Claude 重做 → 推送**」的閉環自動化。V3.10 補上「最後檢查 N 天前」讓沒新版時也能確認爬蟲還活著。V3.11 完整跑過第一次健保更新流程 (115/4/23 → 115/5/22),意外撿回 V3.9 parser 漏抓的 2 個歷史藥。V3.12 加上健保署下載頁連結,讓 Sela 找下次更新 PDF 不用再 Google。下版重點仍是 parser 邊界檢測 (避免條文溢出合併)。

V4.0.0 把 115/6/1 更新用「兩層 diff」重做一遍 (編號層 + 條文層),抓回 V3.11 漏的 10+1 個內部條文修訂(含 9.69 術前輔助 nivolumab/P016);並用「寬驗證」逐藥比對 App vs PDF,順手修掉 V3.9/V3.11 留下的 3 類 bug(search_text 沒重生、9.98 溢出、9.68/9.19/9.56 前言遺失)。從此「兩層 diff + 寬驗證 + search_text 一律重生」成為每次更新的標準流程。

V4.1.0 對齊 SELA Kit（首次），並補上一批個管師日常會用到的前端小功能（複製連結、常用、最近搜尋、副作用照護跳轉、列印優化）。剩下最大一塊是 60 項品質指標附件，因屬不同領域待定放哪再做。

V4.2.0 跟進健保 115/6/23 更新（9.18 Trastuzumab 早期乳癌新規，115/7/1 生效）——此次新版 PDF 首度是乾淨文字檔，兩層 diff 流程照常運作、定位精確。

V5.8.0 修的是這個專案到目前為止**唯一造成過臨床延誤**的錯：胸腔科張醫師回報 9.46 TS-1 選「肺癌」時仍顯示「事前審查」，而健保署原文全段只有一句事審、位置在「2.胃癌」底下——他門診的肺癌病人曾因此被醫院系統擋住、TS-1 開不出來。個管師拿我們的工具去查，**得到的是錯的確認**。全庫盤點後分歧共 5 支（9.34/9.37/9.41/9.46/9.48），全是「老適應症不用事審、後加的新適應症才要」的法規歷史堆疊。根因是層級錯置：`pre_review` 當布林值掛在藥品層，但法規的真實層級是**條文**。修法沒有新增任何資料欄位——事審條文本來就掛在帶癌別標籤的 item 底下，資訊早就在結構裡，改成載入時由條文推導即可，**判準與畫面同源，從此不可能出現「條文沒顯示、標籤卻在」**。呈現照 Sela 拍板：三態標籤（事前審查／部分事審／附表待確認），選了癌別就精確顯示，不需要時**明確寫出否定**而不是沉默。同時修掉更危險的放大點：「複製條文」原本無條件斷言需送審，那段文字會被貼進病歷、離開系統後沒有上下文可以自我修正。

V5.7.2 回應 Sela 的一個好問題：「logo 是不是不要有太小的，愈小你愈容易出錯？」——方向對，但診斷要修正：那兩個錯（分界訂太保守、雷達意境消失）**都與尺寸無關**，一個是掃描線剛好躺在十字線上讓指標永遠讀到 0，一個是我判斷錯了「哪個特徵承載身分」。而且「不做小尺寸」不會消除小尺寸問題，只會把它交給作業系統的縮圖器——那正是最初那團糊的來源。能減少的不是尺寸，是**我在小尺寸上做的獨立決策數量**：所以把兩套幾何收斂成「SVG 是唯一的簡化幾何、所有光柵檔一律由完整版母圖縮」，代價（ico 在 16/32px 稍糊）已量測並明寫。更重要的是流程那條：**≤48px 的視覺資產一律附原寸＋放大對照圖給人看過才算完成**——小尺寸的錯人一眼看得出來，我只能靠指標而指標會騙人。

V5.7.1 修的是我自己造成的問題：CLAUDE.md 明寫「不要手改 cancerTags，跑 derive_v51.py 全庫重推」，但前兩版為了修審稿問題我直接編輯出版檔、繞過了那支腳本。**資料沒壞（重推比對 1,317 個節點、不一致 0），壞的是帳**——571 個 item 的溯源欄位全部流失，害 validate 的警告從 24 筆有效訊號變成 128 筆噪音。所以本版做兩件事：用 `rederive.py` 重建溯源，並加 **BLOCK 14 讓「繞過管線手改標籤」直接擋出版**——**「不要手改 X」這種規則，光寫在文件裡不會被遵守，我自己就是反例。** 順手發現對映表指向的 `MAP_969` 早在 V5.1.0 就不在管線裡了，而我照著那行過期文件寫程式、直接撞上 AttributeError——過期的自我描述文件這次騙到的是 Claude 自己。

V5.7.0 把審稿建議的「機械式全文差異回歸」做成常設閘門。第一次跑出漏抄 160、疑錯字 167——**幾乎全是誤報**，真正的工作是把五類可解釋差異（日期括號、條號歸屬、跨行標題行、父子合併、我們自己的顯示層正規化與註記）逐一扣掉，收斂到 **0／0／0**。壓到 0 才是重點：**此後任何一筆新出現的差異都是真的**，所以它才能掛在 validate 當 BLOCK 13。過程中撿到兩個真錯，其中 **9.5 父項目條文停在 115/6/1 之前的舊版**最嚴重——子項目 9.5.1 早就更新了，害「未選癌別」讀到舊條文、「選婦癌」看不到 dostarlimab 的子宮內膜癌條文，正是坑 #36 的實證；已改成父項目一律由子項目重建。另外補完審稿列的三項功能修正，其中「只看常用」原本是渲染後用 CSS 隱藏，導致統計數字與 pill 計數說謊——**任何影響「有幾筆」的條件都必須進唯一的過濾函式**。

V5.6.0 處理外部審稿的 6 項內容意見，逐項核查到健保署原文後：**4 項屬實照改、2 項歸因錯誤但點出真問題**。屬實的是指數 9 遺失（9.55／9.114，順手又掃到 6 處 `/ mm3`）、9.86 條號位移（全庫掃描發現不只 9.86，是 9.19／9.68／9.86 三支）、revisions 缺 9 項（全對，改為全文日期聯集共補 17 筆）、刪除條號政策不一致（補回 9.7／9.89，藥品數 151→153）。歸因錯誤的兩項更有價值：**9.69 少 `Alimta Avos` 是健保署原文自己與 9.26 不一致**，不是我們抄錯——改條文就破壞逐字忠實，所以掛橘色交叉差異註記；**P151 六欄全「尚未給付」是對的**，因為官方表頭根本沒有 dostarlimab 欄位，但畫面會讓人誤以為 EC 沒有 ICI 給付，所以加列註記說明「查不到 ≠ 答案是否定的」。三項新的 validate BLOCK（藥名溯源／條號對齊／官方條號完整收錄）讓這三類問題以後不會再溜過。

V5.5.0 把 Sela 用 Gemini 生的 app logo 套進專案，走 Kit logo §10.2 工作流 B。過程證明「AI 生成的 logo 拿來就能用」幾乎不成立：原圖主體只佔畫布 6%（生圖 AI 在圓角方形內留了大片留白），而且線稿風的同心圓靶心在 ≤48px **完全糊成一塊**——我用「右半水平中線的明暗交替次數」量化，完整版 16/24/32/48px 全是 0 次，64px 才出現。所以依 §10.4 做了兩套母圖：**≤48px 用簡化實心剪影版（實心白膠囊 + 負空間圓環 + 中心點）、≥64px 用完整版**，兩套由同一組幾何常數產生所以氣質一致。去背也不是四角 floodfill 就好——圓角邊界殘留的抗鋸齒淺色像素會讓「亮度抓 bbox」抓到整個方形，必須用連通區域分離「外白底 vs 主體白色」。雙軌落地：favicon／入口頁／工具列全換 app logo，SELA logo 只留在「關於」底部歸屬印記（V5.4.0 先建好的獨立引用，這版換 favicon 才沒破功）。

V5.4.2 把「複製連結」換成「複製條文」。Sela 的理由一句話點破設計盲點：**不是每個人都有這程式的權限**——深連結預設接收端在同一個系統裡，但個管師要傳條文的對象是醫師、藥師、病房，他們沒有密碼。跨組織溝通的通用貨幣是純文字。新功能產出的文字**跟著目前的癌別篩選**（選肺癌時 9.69 只複製 8 條、7,229 字），保留〔通用〕標記與「另有其他癌別條文 N 項未列入」讓接收端知道這是節錄，PD-L1 對照表轉成逐列文字，並**自動附上資料公告／生效日期與出處**——文字一離開系統就沒有畫面上的資料日期，這行是防止過期條文被當成現行規定的唯一線索。

V5.4.1 修掉手機上「版面跑掉 + 星號跑到字中間」。版面的根因是 `.subitem-text` / `.clause-text` 有 `flex:1` 卻沒 `min-width:0`——V5.2.0 把 `min-width:720px` 的 PD-L1 對照表放進子項後，flex 子項拒絕縮小、整頁被撐開，而且**外層不肯縮，內層的橫向捲動就永遠用不到**。這是坑 #34 的第二次踩（V4.1.2 是同一個陷阱的縱向版）。星號則有兩個來源：PD-L1 表的註記符號 `SP263*` 與 9.103 的 `2*106`（原文用 `*` 當乘號）——半形 `*` 在中文段落換行後會被讀成錯字，顯示層一律改成上標 `※` 與 `×10ⁿ`，資料層不動。順手把 151 支藥各一顆、卻都連到同一個網址的「副作用照護」按鈕收成「關於→相關資源」的單一入口。

V5.4.0 把專案對齊到 SELA Kit V1.27.0（上次對齊的是 V1.9.0，落差 18 個 b 版）。有意思的是**這次對齊的收穫不是補格式**：Kit V1.27.0 已經把本專案 V5.1.0 的教訓收成 Kit 級坑 #83（衍生資料兩鐵則）與 #7 補強，所以「回流」這端已經閉環；真正撿到的是**用一份新清單重看自己時發現的三件舊債**——「關於本系統」面板還停在 V4.x 的「逐癌切片」世界觀（架構改了三版、說明文字沒人動，是坑 #3「資料與顯示同源」最難察覺的變體）、`.gitignore` 在保護早已不存在的幽靈檔、兩處 catch 區塊的 `console.log`。優化體檢 4 條 OPT 中 3 條技術不適用、OPT-4 已實質落實，**0 項可升級**。另依 Kit §8.3 鐵律產出 `SELA-logo-prompt.md`，並先把 SELA 歸屬印記建在「關於」面板底部（引用獨立圖檔），這樣日後 favicon 換成 app logo 時雙軌不會破功。

V5.3.0 是 V5.2.0 的收尾稽查：既然 9.69 的表格被壓平那麼久沒人發現，就得回答「還有沒有第二張」。用「重複片語次數 × 短行比例」掃過 153 個段落，**確認全文只有 9.69 一張表格**。但同一次逐字元對比抓到另外三類遺失——最要緊的是 **9.20 Rituximab 的「用於抗癌瘤部分」**：這支藥同時有非癌症適應症（ANCA 血管炎），少了這句話會誤讀整節適用範圍。另外把 716 筆從來沒存過的藥品層歷次修訂生效日補回來。

V5.2.0 處理的是使用者實際感受到的問題：**畫面雜亂**。追下去發現不是排版，是 9.69 那張 29×6 的 PD-L1／事審代碼對照表在 PDF 轉文字時被壓成 3,901 字一段——而且六欄裡通常只有一兩欄真的有給付，其餘全是「本藥品尚未給付於此適應症」的雜訊。這也回答了「為什麼當初要手工做 per_cancer 摘要」：因為原文結構壞了。本版把表格結構解析回來（事審代碼當錨點、封閉的儲存格用語、round-trip 逐字驗證），每一列自己帶癌別標籤，畫面上只列出真的有給付的藥品與其 PD-L1 門檻，原表收進可展開的對照表。選肺癌從 29 列變 8 列。

V5.1.0 是 V5.0.0 的必要下半場。V5.0.0 把架構改對了(前端只讀原文),但標籤是從舊切片反推的——外部審稿一眼看出「架構已對、資料搬移未完成」,並抓到 9.2/9.10/9.16 等一批誤標。本版把標籤改成**從健保署原文重新推導**:先辨識行政共通條文標 `["*"]`,再用病名同義詞表歸癌別,標不到才保守落 `["*"]`(且與行政條文分流記帳)。真正的關鍵不是關鍵字表,而是一條語意規則:**標題指明癌別的條文,子項是它的條件;標題沒指明癌別的傘狀條文,子項才是各自獨立的適應症**。同時把 44 筆被截斷的修訂日期與 14 處遺失的上標數值從原文回填,並補上一支「BLOCK 就不出版」的建置端驗證腳本。

V5.0.0 動了三年沒動過的資料模型：**把「癌別另存一份切片」改成「每條原文貼癌別標籤」**。這不是效能或版面優化，是把系統最危險的失敗模式翻過來——舊模型切片漏一條，個管師畫面上就少一條給付條件且毫無提示；新模型最壞情況只是多顯示幾條通用條文。順手撿回 4 條舊切片漏掉的條文（9.2 兩條、9.5.1、9.69 婦癌）、修掉 9.48 混進 9.49 前列腺癌條文的資料污染，並讓檔案瘦身約 14%。下一步是把「一條混多癌」的條文做到子項級標籤全覆蓋。
