# SELA-logo-prompt.md — Cancer Drug（健保癌症藥物速查系統）

> 由 Claude 依 SELA-Starter-Kit V1.27.0 §17 自動工作流產出。
> 給其他 AI（Gemini / Midjourney / DALL-E / Adobe Firefly 等）生 logo 用。

---

## 〇、產出資訊（讓 SELA 驗證）

- **專案名稱：** Cancer Drug（健保癌症藥物速查系統）
- **產出日期：** 2026-07-30
- **使用 Kit 版本：** V1.27.0
- **使用範本：** B 醫療專業型
- **資訊來源：** 從專案 CLAUDE.md 自動萃取

## 一、萃取的設計 context

- **這 app 做什麼：** 查健保署「藥品給付規定第 9 節—抗癌瘤藥物」，選定癌別後只顯示該癌別相關的給付條文。
- **給誰用：** 醫院癌症中心的癌症個案管理師（3 人）。
- **解決什麼痛點：** 給付條文超過 24,000 字、跨癌別藥物一條混多癌（9.69 免疫檢查點抑制劑最長），在原文裡找自己負責癌別的條件會迷路、也容易漏看共通條件。
- **情緒基調：** 嚴謹、機構感、低調（臨床查核用途，不能有玩具感；失敗方向必須是「多顯示」而非「藏條」）。
- **使用情境：** 醫師已決定用某支藥後開啟，用來核對給付條件與事前審查要求；也在送審備料、病歷記錄時反覆開關查閱。

## 二、自動決策

### 範本類型
- **選定：** B 醫療專業型
- **理由：** 內容是臨床用藥的健保給付規定、使用場域在醫院癌症中心，臨床屬性重於單純的資料查詢工具（若只看「查詢／對照」會落到 D 資料技術型，但本 app 的判讀對象是病人的治療條件，取 B）。

### 壁虎/蜥蜴傾向
- **傾向：** not a natural fit — 不是命令，只是給 AI 的傾向參考。
- **理由：** 依 §13.2 判斷流程 Q1，本 app 的精神是「精確對照、不漏條」，不是「守護／跨環境／靜默常駐」，壁虎意象與之不契合。
- **註（Kit V1.25.0 §17.2 澄清）：** 此傾向**只寫在本節供 SELA 參考，不寫進第三節的精煉 prompt**——主體一律交給生圖 AI 發揮。

### 背景色提案（圓角填色、背景是白底待後製）
- **選定：** 霧藍 `#436f8a`（第一候選）
- **理由：** 這是本專案已上線驗證的主題色（`:root --primary`），屬 Kit 建議的醫療系統北歐霧藍系；app logo 與介面同色可一眼認出同一產品。且非愛馬仕橘，與 SELA 主 logo 不撞色（§9 同色隔離規則不會被觸發）。
- **第二候選：** 板岩藍 `#1f3a4d`（介面機構標頭色）——機構感更重、縮到 16px 對比更強，但與介面主色不同、辨識連結較弱。

## 三、完整 Prompt（複製這段貼到生圖 AI、依 §14.7 極簡中文版）

```
一個 app icon 的 logo。

這個 app 是什麼：
這個 app 是查健保給付規定裡「抗癌瘤藥物」條文的工具。使用者先選一個癌別，
畫面就只顯示跟那個癌別有關的給付條文，不必在兩萬多字的長條文裡逐條找。
每條條文都標好適用癌別，適用所有癌別的共通用藥條件會另外標示、一併顯示。
也可以用藥名搜尋，並查免疫治療藥物的檢測門檻與送審代碼。

設計要求：
- 圓角方形、1:1 比例
- 扁平向量風格、單一白色主體圖案
- 底色填 #436f8a、放在純白背景上
- 不要漸層、陰影、3D、光暈
- 小尺寸（favicon）也要清楚

主體畫什麼由你發揮、呼應這個 app 的精神即可。
```

> **產出檢查（§14.7 守則 + §15.1 檢核）：** 已確認只有「app 介紹 4 句 + 設計精神 + 一個底色」；
> 無 mood 形容詞、無使用者輪廓、無框線裝飾、無選擇題、無 reference、無壁虎、未指定主體。

## 四、給 SELA 的備註

- **prompt 已要求 AI 輸出乾淨純白底 PNG**（Gemini 生不出透明、白底反而好處理）
- 拿到原圖後 → 回我這邊走 §10.2 四步（取底色 → 四角 floodfill → 滿版方形 → 圓角交顯示端）把白底轉透明 + 生多解析度套組
- **小尺寸請先目視（§10.4，V1.27.0 新增）：** 生成後先渲染 16 / 32 / 48 px 看一眼；線稿風在 16px 幾乎必糊。若 AI 給的是細線稿，我會另做一版**簡化實心剪影**專供 ≤48px 使用（兩套母圖 optical sizing）。
- **prompt 不指定畫什麼主體** — 只讓 AI 認識這個 app。方向不喜歡就跟我說，我可在 prompt 補「avoid X」或「lean toward Y」微調，但仍不寫死。
- 想改範本類型 / 顏色 / 壁虎傾向 → 告訴我「改 X 為 Y」，我重產這份檔（不需要改 CLAUDE.md）。
- **落地時的雙軌配置（§9）**，等 app logo 定案後我一次改：
  | 位置 | 放什麼 |
  |---|---|
  | `favicon/`（分頁 / 手機捷徑 / webmanifest icons）| app logo |
  | 入口密碼頁 + 頂部工具列的主視覺 | app logo |
  | 「關於本系統」底部歸屬印記 | SELA logo（**V5.4.0 已先建好**，引用獨立的 `favicon/sela.svg`，不重用 favicon 引用）|
  | `site.webmanifest` 的 `theme_color` | app 主題色 `#436f8a` |
